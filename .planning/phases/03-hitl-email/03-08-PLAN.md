---
phase: 03-hitl-email
plan: "08"
type: execute
wave: 5
depends_on: ["03-01", "03-06"]
files_modified:
  - backend/app/agent_builder/api/v1/instances.py  # 增 GET /<id>/tracking
  - backend/app/agent_builder/services/instance_service.py  # 增 get_tracking_for_applicant
  - backend/app/agent_builder/schemas/tracking.py
  - web/src/app/dashboard/instances/[id]/tracking/page.tsx
  - web/src/components/instance/tracking-timeline.tsx
  - web/src/components/instance/applicant-only-records.tsx
  - web/src/lib/api/instances.ts  # 添加 fetchTracking
  - backend/tests/test_tracking_api.py
  - web/tests/tracking-timeline.spec.tsx
  - docs/reading-dify-03-08-tracking-page-2026-05-17.md
autonomous: true
requirements:
  - HITL-07
must_haves:
  truths:
    - "GET /api/agent_builder/v1/instances/<id>/tracking 仅 applicant_id == current_user.id 才放行（否则 403）"
    - "返回 schema：{instance_id, status, applicant: {id, name}, current_node: {id, title, status, actor: {email}, deadline_at}, records[]}"
    - "records 中只暴露 actor_email/name + action + reason + ts；不暴露 IP/UA（CONTEXT §申请人追踪页隐私）"
    - "/dashboard/instances/<id>/tracking 页面登录用户可访问"
    - "若 current_user 是 admin → 显示完整 records（含 IP/UA）；若非 admin 但 applicant → 仅基础字段"
    - "时间线 UI：竖向时间线，每条节点 status + actor + action + ts；当前节点高亮 + 截止倒计时"
    - "10+ 测试通过（API + UI）"
  artifacts:
    - path: "backend/app/agent_builder/services/instance_service.py"
      provides: "get_tracking_for_applicant 方法（NEW）"
    - path: "web/src/app/dashboard/instances/[id]/tracking/page.tsx"
      provides: "申请人追踪页"
  key_links:
    - from: "backend/app/agent_builder/api/v1/instances.py"
      to: "backend/app/agent_builder/services/instance_service.py"
      via: "get_tracking_for_applicant"
      pattern: "applicant_id check + WorkspaceScopedQuery"
---

<objective>
HITL-07 申请人追踪页 — 已登录用户能在 dashboard 看到自己提交的实例的当前节点状态、历史 records 时间线。

Purpose:
- HITL-07：申请人流程追踪页（提交人可看自己实例的状态与历史）

Output: 1 backend API endpoint + 1 service method + 1 dashboard 页 + 2 React 组件 + 10+ 测试。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hitl-email/03-CONTEXT.md
@.planning/phases/03-hitl-email/03-RESEARCH.md
@.planning/phases/03-hitl-email/03-01-PLAN.md  # node_states.payload 已含 records
@backend/app/agent_builder/api/v1/instances.py  # Phase 2 instances API 已有
@backend/app/agent_builder/services/instance_service.py  # Phase 2 已有

<task_0_reading_doc_gate>

**CLAUDE.md 2.7 GATE**

**必读 Dify 模块**：
- `/Users/admin/ai/ref/dify/repo/api/controllers/console/app/workflow_run.py`（实例详情 + step list）
- 前端：`/Users/admin/ai/ref/dify/repo/web/app/components/app/workflow-log/`（time-line UI 参考）

**Reading doc 路径**：`docs/reading-dify-03-08-tracking-page-2026-05-17.md`

**必含小节**：6 节 + **§7 申请人 vs admin 数据脱敏策略**

</task_0_reading_doc_gate>

</context>

<tasks>

<task type="auto">
  <name>Task 0: Reading doc gate</name>
  <files>
    - docs/reading-dify-03-08-tracking-page-2026-05-17.md
  </files>
  <action>
    1. Read Dify workflow_run.py + workflow-log 前端
    2. 写 reading doc 含 §7 数据脱敏策略
    3. Commit doc
  </action>
  <verify><automated>test -f docs/reading-dify-03-08-tracking-page-2026-05-17.md</automated></verify>
  <done>Reading doc 已 commit。</done>
</task>

<task type="auto">
  <name>Task 1: Backend GET /<id>/tracking endpoint + InstanceService.get_tracking_for_applicant + 集成测试</name>
  <files>
    - backend/app/agent_builder/api/v1/instances.py  # 加 endpoint
    - backend/app/agent_builder/services/instance_service.py  # 加 method
    - backend/app/agent_builder/schemas/tracking.py
    - backend/tests/test_tracking_api.py
  </files>
  <action>
    1. 写 `schemas/tracking.py`：
       ```python
       class TrackingRecord(BaseModel):
           actor_email: str
           actor_name: str | None
           action: str
           reason: str = ""
           ts: str
           # admin only
           ip: str | None = None
           ua: str | None = None
       class TrackingResponse(BaseModel):
           instance_id: UUID
           status: str
           applicant: dict
           current_node: dict | None
           records: list[TrackingRecord]
       ```
    2. 在 `instance_service.py` 添加 `get_tracking_for_applicant`：
       - 查 FlowInstance（含 workspace_id 隔离）
       - 校验 `current_user.id == instance.creator_id` 或 user.role == admin → 否则抛 PermissionError
       - 查 NodeState 列表 → 找当前 `pending/waiting_human/in_review` 节点
       - 聚合所有 node_state.payload.records → 排序输出
       - 若 user 非 admin → 脱敏 IP/UA 字段为 None
    3. 在 `api/v1/instances.py` 添加 endpoint：
       ```python
       @router.get("/instances/{instance_id}/tracking", response_model=TrackingResponse)
       async def get_tracking(instance_id: UUID, ...):
           ...
       ```
    4. 测试 `test_tracking_api.py`（8 用例）：
       - test_applicant_can_view_own_instance_tracking
       - test_non_applicant_403：B 用户访问 A 创建的实例 → 403
       - test_admin_can_view_anyone：admin 角色绕过 applicant 检查
       - test_admin_sees_ip_and_ua_in_records
       - test_applicant_does_not_see_ip_or_ua：返回值中 ip/ua 字段为 None
       - test_cross_workspace_returns_404：B workspace 用户访问 A workspace 的 instance → 404
       - test_current_node_includes_deadline_for_active_hitl
       - test_completed_instance_has_no_current_node
  </action>
  <verify><automated>cd backend && pytest tests/test_tracking_api.py -x --tb=short</automated></verify>
  <done>Backend endpoint + 8 测试通过。</done>
</task>

<task type="auto">
  <name>Task 2: 前端 dashboard 追踪页 + 2 组件</name>
  <files>
    - web/src/app/dashboard/instances/[id]/tracking/page.tsx
    - web/src/components/instance/tracking-timeline.tsx
    - web/src/components/instance/applicant-only-records.tsx
    - web/src/lib/api/instances.ts  # 添加 fetchTracking
    - web/src/lib/types/tracking.ts
    - web/tests/tracking-timeline.spec.tsx
  </files>
  <action>
    1. `types/tracking.ts`：TrackingResponse / TrackingRecord interface（与 backend schemas 对齐）
    2. `api/instances.ts` 添加 `fetchTracking(instanceId): Promise<TrackingResponse>`
    3. 写 `tracking-timeline.tsx`：竖向时间线，节点 status (icon 区分 done/rejected/returned/in_review) + actor + action + ts；当前节点高亮 + DeadlineCountdown 复用 03-07
    4. 写 `applicant-only-records.tsx`：极简 record 列表组件（actor_email + action chip + reason + ts），不显示 IP/UA（即使 admin 也走另一个 component）
    5. 写 `app/dashboard/instances/[id]/tracking/page.tsx`：调用 fetchTracking + 渲染 timeline；处理 403/404 → 跳 dashboard
    6. 测试 `tracking-timeline.spec.tsx`（4 用例）：
       - test_renders_records_chronological
       - test_current_node_highlighted
       - test_status_icons_correct
       - test_overdue_node_shows_red_deadline
  </action>
  <verify><automated>cd web && pnpm exec vitest run tests/tracking-timeline 2>&1 | tail -5</automated></verify>
  <done>3 组件 + 1 页面 + 4 vitest 测试通过。</done>
</task>

</tasks>

<verification>
- /api/agent_builder/v1/instances/<id>/tracking endpoint 注册
- WorkspaceScopedQuery 隔离（跨 workspace 404）
- 申请人 vs admin 数据脱敏正确
- 12+ 测试通过
</verification>

<success_criteria>
1. 申请人能在 /dashboard/instances/<id>/tracking 看自己实例进度
2. 非 applicant 访问返回 403
3. admin 可看任何实例 + 完整 IP/UA
4. 申请人脱敏看不到 IP/UA
5. 时间线显示历史 + 当前节点高亮 + 倒计时
6. WorkspaceScopedQuery 多租户隔离生效
</success_criteria>

<output>
完成 .planning/phases/03-hitl-email/03-08-SUMMARY.md，含：
- API 契约 + 脱敏策略
- 2 前端组件 API
</output>
