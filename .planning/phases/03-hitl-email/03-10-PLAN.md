---
phase: 03-hitl-email
plan: "10"
type: execute
wave: 6
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07", "03-08", "03-09"]
files_modified:
  - e2e/hitl_email_delivery.spec.ts
  - e2e/hitl_token_login.spec.ts
  - e2e/hitl_safe_links_bot.spec.ts
  - e2e/hitl_token_invalidation.spec.ts
  - e2e/hitl_tracking_page.spec.ts
  - e2e/helpers/hitl-builder.ts
  - e2e/helpers/mailhog-client.ts  # 修改：扩展 HITL 邮件解析
  - e2e/pages/hitl.page.ts
  - e2e/pages/tracking.page.ts
  - docs/reading-dify-03-10-e2e-2026-05-17.md
autonomous: true
requirements:
  - HITL-01
  - HITL-03
  - HITL-05
  - HITL-07
  - NOTI-01
  - NOTI-08
  - NOTI-09
  - NOTI-10
  - AUTH-04
  - AUTH-05
  - NET-05
  - NODE-02
  - NODE-07
must_haves:
  truths:
    - "5 个 Playwright spec 覆盖 ROADMAP Phase 3 全部 5 个 success criteria"
    - "Smoke mode（默认）自动 skip；RUN_E2E=1 跑全部；E2E_FULL_STACK=1 跑含 docker 重启的恢复测试"
    - "复用 Phase 1 mailhog-client.ts + Phase 2 dsl-builder.ts；新增 hitl-builder.ts 生成含 HITL 节点的 DSL"
    - "Page Object: hitl.page.ts（公网决策页 — 不需要登录）+ tracking.page.ts"
    - "每 spec 头注释明示覆盖 ROADMAP Phase 3 #N criterion"
    - "Safe Links bot regression：用 page.setExtraHTTPHeaders({'User-Agent': OUTLOOK_SAFELINKS_BOT_UA}) 模拟扫描器 GET"
    - "断言 jti 未消费：spec 中查 backend API（或直接连 DB）确认 hitl_tokens.used_at IS NULL"
    - "完整 path: 启动 workflow（含 HITL 节点）→ mailhog 收邮件 → 提取 token → POST 提交 → 流程推进 → tracking 页可见"
  artifacts:
    - path: "e2e/hitl_email_delivery.spec.ts"
      provides: "ROADMAP #1 验收 — 4 button 邮件"
      contains: "describe.*email.*4.*button"
    - path: "e2e/hitl_token_login.spec.ts"
      provides: "ROADMAP #2 验收 — token 即登录决策推进"
    - path: "e2e/hitl_safe_links_bot.spec.ts"
      provides: "ROADMAP #3 验收 — Safe Links GET 不消费 jti"
    - path: "e2e/hitl_token_invalidation.spec.ts"
      provides: "ROADMAP #4 验收 — token 失效 + 409"
    - path: "e2e/hitl_tracking_page.spec.ts"
      provides: "ROADMAP #5 验收 — 申请人追踪"
    - path: "e2e/helpers/hitl-builder.ts"
      provides: "DSL builder 含 HITL 节点（form_schema + assignees）"
    - path: "e2e/pages/hitl.page.ts"
      provides: "公网决策页 Page Object"
    - path: "e2e/pages/tracking.page.ts"
      provides: "追踪页 Page Object"
---

<objective>
**Phase 3 E2E gate** — 5 个 Playwright spec 覆盖 ROADMAP.md Phase 3 全部 5 个 success criteria。从真实浏览器视角端到端验证：邮件投递 → 链接点击 → token 即登录 → 表单提交 → 流程推进 → 追踪页可见。

**Critical**：Safe Links bot regression（ROADMAP #3）是 CLAUDE.md 2.5 P0 验证项。

Purpose: Phase 3 全 13 requirements 端到端覆盖。

Output: 5 spec + 3 helpers + 2 page objects + Smoke/Standard/Full 三模式。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-hitl-email/03-CONTEXT.md
@.planning/phases/03-hitl-email/03-RESEARCH.md  # §八 Validation Architecture 5 criteria
@.planning/phases/03-hitl-email/03-01..09-PLAN.md  # 全部前置 plan
@.planning/phases/01-skeleton/01-06-SUMMARY.md  # E2E 基础设施
@.planning/phases/02-dsl/02-10-SUMMARY.md  # Phase 2 E2E 模式参照
@e2e/playwright.config.ts  # Phase 1 已配
@e2e/helpers/api-client.ts  # Phase 1 fetch wrapper
@e2e/helpers/mailhog-client.ts  # Phase 1 已写

<roadmap_phase3_criteria>
ROADMAP.md Phase 3 success criteria（本 plan 必须覆盖）：
1. 审批人收到包含"同意/退回/拒绝"按钮的邮件，每个按钮有独立 token 深链
2. 审批人点击链接后无需登录账号即可看到决策表单，填写并提交后流程推进
3. Outlook Safe Links 扫描器 GET token 链接不消费 jti，审批人首次点击仍可正常决策
4. 同一 token 提交后立即失效；同节点其他 token 同时失效；重复提交返回 409
5. 申请人能在追踪页查看自己实例的当前节点状态和历史决策记录
</roadmap_phase3_criteria>

<run_modes>
| 模式 | 触发 | 跑哪些 spec |
|---|---|---|
| Smoke | 默认 | 全部自动 skip |
| Standard | RUN_E2E=1 + docker compose up | 5 spec 全跑 |
| Full | E2E_FULL_STACK=1 + docker compose up | 5 spec + arq worker 超时催办验证 |
</run_modes>

<task_0_reading_doc_gate>

**CLAUDE.md 2.7 GATE — E2E 终结性 plan**

**必读**：本 plan 不读新 Dify 代码，但 reading doc 必须汇总：
1. 03-01 ~ 09 reading docs 索引（list 9 个 docs + 各自借鉴点）
2. Phase 1/2 E2E 模式总结（mailhog 用法 + Page Object Pattern + RUN_E2E flag）
3. Safe Links bot UA 测试模式（与 03-03 测试对齐）

**Reading doc 路径**：`docs/reading-dify-03-10-e2e-2026-05-17.md`

</task_0_reading_doc_gate>

</context>

<tasks>

<task type="auto">
  <name>Task 0: Reading doc gate（汇总型）</name>
  <files>
    - docs/reading-dify-03-10-e2e-2026-05-17.md
  </files>
  <action>
    汇总 Phase 3 全 10 plans 的 reading docs + 测试模式索引。Commit 后才能写 E2E spec。
  </action>
  <verify><automated>test -f docs/reading-dify-03-10-e2e-2026-05-17.md</automated></verify>
  <done>Reading doc 已 commit。</done>
</task>

<task type="auto">
  <name>Task 1: hitl-builder helper + 2 Page Objects + mailhog 扩展</name>
  <files>
    - e2e/helpers/hitl-builder.ts
    - e2e/helpers/mailhog-client.ts  # 修改
    - e2e/pages/hitl.page.ts
    - e2e/pages/tracking.page.ts
  </files>
  <action>
    1. `hitl-builder.ts`：
       - `buildHitlDsl({applicantEmail, approverEmail, formSchema})`：3 节点 Start → HITL → End，含 form_schema with text/select/textarea 字段
       - `buildHitlSequenceDsl(...)`: 2 个串联 HITL（Phase 4 用，本 plan 留接口）
    2. 扩展 `mailhog-client.ts`：
       - `getLatestHitlEmail(recipient)` → 返回 {to, subject, html, deeplinks: [{action, url}]}（解析 HTML 中 3 个 button）
       - `extractTokensFromEmail(html)` → [{action, jti}]
    3. `hitl.page.ts`（Page Object — 公网决策页）：
       - `goto(token)` → `/hitl/<token>`
       - `getActionButtons()` → 返回 button list
       - `fillFormField(name, value)` / `fillReason(text)`
       - `submitAction(actionId)` → click + 等成功页 / 错误页
       - `getCookieByName(name)` → 验证 hitl_session_<jti> 已设
       - `getErrorMessage()` → 错误页文本
    4. `tracking.page.ts`：
       - `goto(instanceId)` → `/dashboard/instances/<id>/tracking`
       - `getCurrentNodeStatus()` / `getRecordsTimeline()` / `getApplicantInfo()`
       - `expectNoIpUaVisible()` → 申请人视角断言 IP/UA 不显示
  </action>
  <verify><automated>test -f e2e/helpers/hitl-builder.ts && test -f e2e/pages/hitl.page.ts && test -f e2e/pages/tracking.page.ts</automated></verify>
  <done>3 文件写就；TypeScript 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 5 个 Playwright spec</name>
  <files>
    - e2e/hitl_email_delivery.spec.ts
    - e2e/hitl_token_login.spec.ts
    - e2e/hitl_safe_links_bot.spec.ts
    - e2e/hitl_token_invalidation.spec.ts
    - e2e/hitl_tracking_page.spec.ts
  </files>
  <action>
    **每 spec 头注释**：`// ROADMAP Phase 3 #N: <criterion>` + `test.skip(() => !process.env.RUN_E2E && !process.env.E2E_FULL_STACK, '需要 docker compose up + RUN_E2E=1')`

    **hitl_email_delivery.spec.ts** (ROADMAP #1)：
    - test：发布含 HITL 节点的 workflow → POST /instances 启动实例 → mailhog 收到邮件
    - 断言：邮件主题含流程名 + 节点名；HTML 含 3 个 `<a href>` button（颜色：绿/黄/红）；plain text fallback 含 3 个 URL
    - 断言：subject 不含 "[催办]" 前缀（首次发送）

    **hitl_token_login.spec.ts** (ROADMAP #2)：
    - test：mailhog 提取 token → 浏览器访问 `/hitl/<token>` (page1)
    - 断言：返回决策页 HTML + Set-Cookie 含 hitl_session_<jti>
    - 断言：jti 未消费（GET 不消费 — 通过 admin API 查 backend `/api/admin/hitl-tokens/<jti>` 或直接连 DB）
    - test：填表单 + 点 "通过" → POST → 成功页
    - 断言：instance.status 推进 + node_state.status='done'

    **hitl_safe_links_bot.spec.ts** (ROADMAP #3 + CLAUDE.md 2.5 P0)：
    - 4 种 UA 测试（parametrize）：Outlook Safe Links / Microsoft Defender / Slackbot / Googlebot
    - 每个 UA：模拟扫描器 GET 链接 → 200 + bot_scan.html 内容 + 无 Set-Cookie
    - 断言：jti 仍未消费（DB 查或 admin API）
    - test：扫描器走后，真实用户用同 token 访问 → 仍可见决策页（jti 未被消费）+ 可正常提交

    **hitl_token_invalidation.spec.ts** (ROADMAP #4)：
    - test：3 token 邮件 → 用户提交 token1 → token2/token3 立即失效
    - 断言：token2/token3 用 GET 访问 → 410 "决策已提交，链接失效"
    - test：用户用同一 token 重提交 → 409 "决策已提交，无法重复提交"
    - 断言：node_state.status 不被二次提交修改
    - test：advisory_lock 并发模拟：两个 Page 并行 click 提交 token1 + token2 → 仅一个成功（另一个 409）

    **hitl_tracking_page.spec.ts** (ROADMAP #5)：
    - test：applicant 登录 → 进入 dashboard → 启动 workflow → 跳 tracking 页
    - 断言：current_node 显示当前 HITL 节点 + actor email + deadline 倒计时
    - 断言：HITL 节点完成后 records 显示历史决策（actor + action + ts）
    - 断言：applicant 视角看不到 IP/UA（applicant-only-records 组件）
    - test：B 用户访问 A 的 tracking → 403
    - test：admin 访问 tracking → 看到完整 IP/UA（tracking-timeline 含 admin only 字段）
  </action>
  <verify>
    <automated>
    ls e2e/hitl_*.spec.ts | wc -l | grep -q "5" &&
    cd e2e && npx playwright test --list 2>&1 | grep -E "hitl_|ROADMAP" | head -10
    </automated>
  </verify>
  <done>5 spec 写就；playwright --list 能枚举；smoke 自动 skip。</done>
</task>

<task type="auto">
  <name>Task 3: SUMMARY + 更新 STATE / ROADMAP / REQUIREMENTS</name>
  <files>
    - .planning/phases/03-hitl-email/03-10-SUMMARY.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    1. 写 `03-10-SUMMARY.md`：
       - 5 spec ↔ 5 ROADMAP criteria 追溯表
       - 13 requirements ↔ 10 plans 追溯表
       - 运行模式（Smoke / Standard / Full）
       - 遗留：reminder 24h/48h/72h 真实计时验证留 manual smoke
    2. `STATE.md`：last_completed_plan = 03-10 + Phase 3 status = complete
    3. `ROADMAP.md`：Phase 3 plan progress 10/10
    4. `REQUIREMENTS.md`：标记 13 个 req 为 Complete
  </action>
  <verify><automated>test -f .planning/phases/03-hitl-email/03-10-SUMMARY.md && grep -q "10/10" .planning/ROADMAP.md</automated></verify>
  <done>SUMMARY + 三文档更新。</done>
</task>

</tasks>

<verification>
- 5 spec 文件存在；playwright --list 能枚举
- Smoke 模式 `bash scripts/run_all_tests.sh` 三层基线照常过（E2E skip）
- RUN_E2E=1 跑通（前提：docker compose up 起全套服务，含 mailhog + arq worker）
- ROADMAP Phase 3 5 criteria 每个有 ≥ 1 spec 覆盖（头注释明示）
- CLAUDE.md 2.5 P0 验证：Safe Links bot regression 在 hitl_safe_links_bot.spec.ts 中完整覆盖
- 共享 Phase 1/2 helpers（mailhog-client + api-client + page object pattern）
</verification>

<success_criteria>
1. 5 Playwright spec 覆盖 ROADMAP Phase 3 全 5 criteria
2. Page Object: hitl.page.ts + tracking.page.ts（复用 + 公网决策页 vs dashboard 追踪页区分）
3. mailhog-client 扩展支持 HITL 邮件解析
4. Safe Links bot regression 覆盖 4 种 UA（Outlook / Defender / Slackbot / Googlebot）
5. CLAUDE.md 2.5 P0 验证：所有 spec assert jti 未被 GET 消费
6. SUMMARY 含 13 requirements ↔ 10 plans 追溯表
</success_criteria>

<output>
完成 .planning/phases/03-hitl-email/03-10-SUMMARY.md + 三文档更新：
- 5 spec ↔ 5 criteria 表
- 13 requirements ↔ 10 plans 表
- Phase 3 完成度声明 → Phase 4（审批链 + IM 通知）可启动
</output>
