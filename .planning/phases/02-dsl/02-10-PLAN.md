---
phase: 02-dsl
plan: "10"
type: execute
wave: 6
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08", "02-09"]
files_modified:
  - e2e/dsl_canvas_drag.spec.ts
  - e2e/instance_run_realtime.spec.ts
  - e2e/instance_checkpoint_recovery.spec.ts
  - e2e/instance_list_filter.spec.ts
  - e2e/dsl_validation_ui.spec.ts
  - e2e/helpers/dsl-builder.ts
  - e2e/pages/canvas.page.ts
  - e2e/pages/instance.page.ts
autonomous: true
requirements:
  - EDIT-01
  - EDIT-02
  - EDIT-03
  - NODE-01
  - NODE-03
  - NODE-05
  - NODE-06
  - EXEC-01
  - EXEC-02
  - EXEC-03
  - EXEC-04
  - EXEC-05
must_haves:
  truths:
    - "5 个 E2E spec 覆盖 ROADMAP Phase 2 全部 5 个 success criteria"
    - "spec 用 webapp-testing skill (Playwright), 与 Phase 1 e2e/playwright.config.ts 同栈"
    - "默认 smoke (RUN_E2E=0) 自动 skip; RUN_E2E=1 跑全部; E2E_FULL_STACK=1 跑含 docker 重启的 checkpoint recovery"
    - "Page Object Model: e2e/pages/canvas.page.ts + instance.page.ts"
    - "DSL builder helper: e2e/helpers/dsl-builder.ts 生成预设 DSL JSON (避免每个 spec 都拼字符串)"
  artifacts:
    - path: "e2e/dsl_canvas_drag.spec.ts"
      provides: "ROADMAP #1 验收"
      contains: "describe.*拖拽.*5 节点"
    - path: "e2e/instance_run_realtime.spec.ts"
      provides: "ROADMAP #2 验收 (SSE 实时状态)"
      contains: "EventSource"
    - path: "e2e/instance_checkpoint_recovery.spec.ts"
      provides: "ROADMAP #3 验收 (重启恢复)"
      contains: "docker.*restart|recover"
    - path: "e2e/instance_list_filter.spec.ts"
      provides: "ROADMAP #4 验收"
      contains: "filter|search|pagination"
    - path: "e2e/dsl_validation_ui.spec.ts"
      provides: "ROADMAP #5 验收"
      contains: "成环|invalid"
  key_links:
    - from: "e2e/instance_run_realtime.spec.ts"
      to: "backend/app/agent_builder/api/v1/instances.py"
      via: "SSE endpoint"
      pattern: "/instances/.*/events"
    - from: "e2e/dsl_canvas_drag.spec.ts"
      to: "web/src/app/dashboard/canvas/page.tsx"
      via: "Canvas 编辑器"
      pattern: "data-testid.*node"
---

<objective>
**Phase 2 E2E gate** — 5 个 Playwright specs 覆盖 ROADMAP.md Phase 2 全部 5 个 success criteria，从真实浏览器视角端到端验证。

**Purpose**：
- 验证 Plan 02-01 ~ 02-09 的输出能组装出可跑的工作流
- 与 Phase 1 E2E 同栈，可复用 helpers/api-client.ts + mailhog-client.ts
- 标识失败点，避免 phase verify 出错

**Output**：5 spec + 2 helpers + 2 page objects；smoke 默认 skip，RUN_E2E=1 跑全部。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/02-dsl/02-CONTEXT.md
@.planning/phases/02-dsl/02-01-PLAN.md (..到 02-09)
@.planning/phases/01-skeleton/01-06-SUMMARY.md (E2E 基础设施参照)
@e2e/playwright.config.ts (Phase 1 配置, 不动)
@e2e/helpers/api-client.ts (Phase 1 已写 fetch wrapper)
@e2e/helpers/mailhog-client.ts (Phase 1 已写)

<roadmap_phase2_criteria>
ROADMAP.md Phase 2 success criteria (本 plan 必须覆盖):
1. 用户能在画布上拖拽 Start/End/LLM/Tool/IfElse 五种节点并连线, 保存为草稿后发布
2. 点击"运行"后实例创建并执行, Web 页面实时显示每个节点的进入/完成状态
3. 服务重启后运行中的实例能从 Postgres checkpoint 恢复继续执行
4. 实例列表页能按工作流/状态过滤, 支持分页搜索
5. DSL 成环或变量引用错误时画布前端拒绝保存并显示具体错误位置
</roadmap_phase2_criteria>

<run_modes>
| 模式 | 触发 | 跑哪些 spec |
|---|---|---|
| Smoke | 默认 | 全部自动 skip |
| Standard | RUN_E2E=1 + docker compose up | canvas_drag + run_realtime + list_filter + validation_ui |
| Full | E2E_FULL_STACK=1 + docker compose up | 全 5 个 (含 checkpoint_recovery, 需要重启容器) |
</run_modes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: helpers + page objects</name>
  <files>
    - e2e/helpers/dsl-builder.ts
    - e2e/pages/canvas.page.ts
    - e2e/pages/instance.page.ts
  </files>
  <action>
    1. e2e/helpers/dsl-builder.ts:
       - `buildSimpleDsl({startToLlmToEnd: boolean}): DslJson` — 生成 3 节点 Linear (Start → LLM(echo) → End)
       - `buildBranchDsl(): DslJson` — 5 节点 (Start → LLM → IfElse → Tool / End-A & End-B)
       - `buildCyclicDsl(): DslJson` — 故意成环用于验证拒绝保存
       - `buildVariableDanglingDsl(): DslJson` — LLM prompt 引用 {{ nonexistent.x }}
    2. e2e/pages/canvas.page.ts: Page Object
       - `goto(workflowId)` → 跳到 `/dashboard/canvas/<id>`
       - `dragNode(type, position)` → 从 palette 拖入节点
       - `connectNodes(fromId, toId)` → 拖动连线
       - `selectNode(nodeId)` → 点击选中
       - `editNodeConfig(field, value)` → 在配置面板写值
       - `saveDraft()` / `publish()` → 点击对应按钮, 等 toast
       - `getIssuePanel()` → 读 Issue 清单
       - `getNodeBorderColor(nodeId)` → 取节点 outline (验证红色)
    3. e2e/pages/instance.page.ts:
       - `goto(instanceId)`
       - `clickRun()` → 启动
       - `getNodeStatus(nodeId)` → poll DOM 取节点状态 (pending/running/done/failed)
       - `waitForInstanceComplete(timeout)`
       - `listenSseEvents(instanceId, timeout)` → 用 EventSource API 直接订阅, 返回事件数组
  </action>
  <verify>
    <automated>
    test -f e2e/helpers/dsl-builder.ts &&
    test -f e2e/pages/canvas.page.ts &&
    test -f e2e/pages/instance.page.ts
    </automated>
  </verify>
  <done>
    Helpers + 2 Page Objects 写好; TS 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 5 个 spec 文件</name>
  <files>
    - e2e/dsl_canvas_drag.spec.ts
    - e2e/instance_run_realtime.spec.ts
    - e2e/instance_checkpoint_recovery.spec.ts
    - e2e/instance_list_filter.spec.ts
    - e2e/dsl_validation_ui.spec.ts
  </files>
  <action>
    每个 spec 头部加注释指明覆盖哪个 ROADMAP criterion。

    **e2e/dsl_canvas_drag.spec.ts** (ROADMAP #1):
    - test: 拖拽 5 种节点 (Start/End/LLM/Tool/IfElse) 到画布
    - test: 连线 Start → LLM → End → 保存草稿 (DB 出现 workflow.draft)
    - test: 发布草稿 → workflow.status=published + version 自增
    - test: 重新打开画布 → 节点 + 连线 + 配置 还原 (DSL roundtrip 验证)

    **e2e/instance_run_realtime.spec.ts** (ROADMAP #2):
    - test: 发布的 workflow 启动实例 (POST /api/instances) → SSE GET /api/instances/<id>/events
    - test: 验证事件序列 node.start (start) → node.complete (start) → node.start (llm) → node.complete (llm) → node.start (end) → instance.complete
    - test: 多浏览器订阅同一 instance 都收到事件 (Redis pub/sub fan-out)

    **e2e/instance_checkpoint_recovery.spec.ts** (ROADMAP #3, FULL_STACK only):
    - test.skip(!E2E_FULL_STACK)
    - test: 启动一个长任务实例 (LLM mock 5s) → 跑到中间 → docker restart api → SSE 重连 → 实例从 checkpoint 续跑完成
    - test: 验证 instance.status = completed, 中间节点不会重复执行 (LangGraph checkpoint 行为)

    **e2e/instance_list_filter.spec.ts** (ROADMAP #4):
    - test: 创建 3 个 workflow, 每个跑 5 个 instance (15 instance), 部分失败 → 实例列表显示 15
    - test: filter status=failed → 仅显示失败的
    - test: filter workflow=<id> → 仅显示该 workflow 的
    - test: 搜索 instance_id 前缀 → 命中
    - test: 分页 page=2 size=10 → 第 11-15 条

    **e2e/dsl_validation_ui.spec.ts** (ROADMAP #5):
    - test: 用 dsl-builder 生成成环 DSL → 通过画布拼出 → 节点边框变红 (canvas.page.getNodeBorderColor)
    - test: Issue 面板出现 error: "节点 X 与 Y 形成环" + 点击跳转到节点
    - test: 点 "发布" → 拒绝, toast 含 "DSL 无效"
    - test: 修复环 (删除某条边) → Issue 消失, 发布成功
    - test: 变量引用 {{ nonexistent.x }} → Issue 出现 "未定义引用 nonexistent.x"

    全部 spec 头部 `test.skip(() => !process.env.RUN_E2E && !process.env.E2E_FULL_STACK, '需要 docker compose up + RUN_E2E=1')`。
  </action>
  <verify>
    <automated>
    ls e2e/dsl_canvas_drag.spec.ts e2e/instance_run_realtime.spec.ts e2e/instance_checkpoint_recovery.spec.ts e2e/instance_list_filter.spec.ts e2e/dsl_validation_ui.spec.ts &&
    cd e2e && npx playwright test --list 2>&1 | tail -10
    </automated>
  </verify>
  <done>
    5 spec 写就; playwright --list 能枚举所有 test; smoke 模式自动 skip
  </done>
</task>

<task type="auto">
  <name>Task 3: SUMMARY + 更新追溯表</name>
  <files>
    - .planning/phases/02-dsl/02-10-SUMMARY.md
    - .planning/STATE.md (last_completed_plan = 02-10)
    - .planning/ROADMAP.md (Phase 2 plan progress)
    - .planning/REQUIREMENTS.md (traceability)
  </files>
  <action>
    1. 写 02-10-SUMMARY.md:
       - 5 spec ↔ 5 ROADMAP criteria 追溯表
       - 运行模式表 (Smoke / Standard / Full)
       - 已知遗留: checkpoint_recovery 需要 docker 重启 (本地 dev 不易跑, CI 自动); LLM 节点用 echo provider mock
    2. STATE.md 更新 last_completed_plan / current_phase
    3. ROADMAP.md Phase 2 plan progress = 10/10
    4. REQUIREMENTS.md traceability 标记 NODE-01/03/05/06 EDIT-01/02/03 EXEC-01..05 = Complete
  </action>
  <verify>
    <automated>
    test -f .planning/phases/02-dsl/02-10-SUMMARY.md &&
    grep -q "10/10" .planning/ROADMAP.md
    </automated>
  </verify>
  <done>
    SUMMARY 写; STATE / ROADMAP / REQUIREMENTS 全部更新
  </done>
</task>

</tasks>

<verification>
- 5 spec 文件存在, playwright config 能枚举 test
- Smoke 模式 `bash scripts/run_all_tests.sh` 三层基线照常过 (E2E 全 skip)
- RUN_E2E=1 跑通 (前提: docker compose up 起 6 服务)
- ROADMAP Phase 2 5 个 criteria 都有 ≥ 1 spec 覆盖 (在 spec 顶部注释明示)
- 与 Phase 1 e2e 共享 playwright.config.ts + helpers
</verification>

<success_criteria>
1. 5 个 Playwright spec 覆盖 Phase 2 全部 5 个 ROADMAP success criteria
2. Page Object Model 抽出 (canvas + instance), 后续 phase 复用
3. DSL builder helper 提供 4 种预设 DSL (linear / branch / cyclic / dangling)
4. Smoke 默认 skip; RUN_E2E / E2E_FULL_STACK 可分级触发
5. 02-10-SUMMARY.md 含 5 criteria 覆盖追溯表
</success_criteria>

<output>
完成 .planning/phases/02-dsl/02-10-SUMMARY.md, 含:
- 5 spec ↔ 5 criteria 表
- 运行模式 (Smoke / Standard / Full) + 触发条件
- Phase 2 完成度声明 → Phase 3 (HITL) 可启动
</output>
