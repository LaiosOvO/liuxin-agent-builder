---
phase: 04_5-bot-triggers
plan: "06"
type: execute
wave: 5
depends_on: ["04_5-05"]
files_modified:
  - docs/reading-browser-harness-04_5-06-2026-05-18.md
  - e2e_v2/conftest.py
  - e2e_v2/helpers/__init__.py
  - e2e_v2/helpers/mattermost_client.py
  - e2e_v2/helpers/bot_test_fixtures.py
  - e2e_v2/specs/conftest_phase_4_5.py
  - e2e_v2/specs/test_phase_4_5_help.py
  - e2e_v2/specs/test_phase_4_5_dm_start_workflow.py
  - e2e_v2/specs/test_phase_4_5_llm_intent_routes_to_status.py
  - e2e_v2/specs/test_phase_4_5_permission_denied.py
  - e2e_v2/specs/test_phase_4_5_help_auto_generated.py
  - e2e_v2/README.md
autonomous: false
requirements:
  - R-IM-01
  - R-IM-02
  - R-IM-03
  - R-IM-04
  - R-IM-05
  - R-IM-06
  - R-IM-07
  - R-IM-11
  - R-IM-12

must_haves:
  truths:
    - "5 个 E2E spec 覆盖 ROADMAP Phase 4.5 全 5 success criteria"
    - "E2E 用 browser-use/browser-harness 通过 CDP 直连 Chrome（用户 2026-05-17 硬性指令 / memory feedback_e2e_browser_harness_only）"
    - "禁用 Playwright / webapp-testing skill（本 plan 严守 browser-harness CDP only）"
    - "Mattermost 容器 fixture: testcontainers-python or docker-py 启动 mattermost/mattermost-team-edition image"
    - "bot.yaml fixture: 用 plugins/bots/test_workflow_bot.yaml (Plan 04_5-02 已创建)"
    - "browser-harness session: 通过 CDP 操作 Mattermost web UI（admin 登录 / 创建 bot account / 发消息 / 验证回复）"
    - "Pitfall 1 回归（E2E）: 发 @bot help → 等待 2s → 断言 channel 内 bot 消息数 ≤ 1（不是 echo loop 触发 2+）"
    - "Pitfall 4 回归（E2E）: 故意触发 handler 异常 → 验证用户收到 '内部错误（事件 ID: ...）' 不含 stack trace"
    - "Pitfall 11 回归（E2E）: 发关键词 → 验证调用 LLM 0 次（通过 bot_audit_logs routed_via='keyword_self_apply'）"
    - "Smoke / Standard / Full 三档运行模式（与 Phase 4 e2e_v2 conftest 一致）"
    - "新增 backend test API `/api/test/bot_audit_logs` 仅 ENABLE_TEST_API=1 时挂载（用于 E2E 断言 audit 字段）"
    - "Phase 5.A 162 platforms + Phase 4 81 IM + Phase 4 e2e_v2 26 specs + Plan 04_5-01~05 130+ 测试 0 regression"
    - "browser-harness reading doc 先于代码 commit（用户硬性指令）"
  artifacts:
    - path: "docs/reading-browser-harness-04_5-06-2026-05-18.md"
      provides: "browser-harness CDP 模式 + Phase 4-12 e2e_v2 模式回顾 + Mattermost web UI 操作笔记"
      min_lines: 80
    - path: "e2e_v2/helpers/mattermost_client.py"
      provides: "MattermostTestClient — admin login / create bot / get channel / get messages (httpx API)"
      contains: "class MattermostTestClient"
    - path: "e2e_v2/helpers/bot_test_fixtures.py"
      provides: "Mattermost container fixture + bot user setup + cleanup"
    - path: "e2e_v2/specs/conftest_phase_4_5.py"
      provides: "Phase 4.5 共用 fixture (mm_container / bot_admin / e2e_required marker)"
    - path: "e2e_v2/specs/test_phase_4_5_help.py"
      provides: "spec 1: @bot help 收到帮助 (ROADMAP #1 + R-IM-03)"
    - path: "e2e_v2/specs/test_phase_4_5_dm_start_workflow.py"
      provides: "spec 2: DM @bot start <wf_id> 启动 workflow (ROADMAP #2 + R-IM-05)"
    - path: "e2e_v2/specs/test_phase_4_5_llm_intent_routes_to_status.py"
      provides: "spec 3: 自然语言 → LLM 路由到 status (ROADMAP #3 + R-IM-04)"
    - path: "e2e_v2/specs/test_phase_4_5_permission_denied.py"
      provides: "spec 4: viewer 角色调 admin-only 命令拒绝 (ROADMAP #4 + R-IM-06)"
    - path: "e2e_v2/specs/test_phase_4_5_help_auto_generated.py"
      provides: "spec 5: help 自动生成 + 加新命令立刻出现在 help (ROADMAP #5 + R-IM-03)"
  key_links:
    - from: "e2e_v2/specs/test_phase_4_5_*.py"
      to: "e2e_v2/helpers/mattermost_client.py + bot_test_fixtures.py"
      via: "fixture mm_container + bot_admin 共享 session"
      pattern: "from helpers.mattermost_client import"
    - from: "e2e_v2/specs/test_phase_4_5_*.py"
      to: "backend test API /api/test/bot_audit_logs"
      via: "断言 routed_via / outcome / latency_ms 字段验证 Pitfall 11"
      pattern: "/api/test/bot_audit_logs"
---

<objective>
Phase 4.5 Wave 5 — E2E gate（browser-harness CDP）。

Purpose:
- 验证 ROADMAP Phase 4.5 全 5 success criteria 端到端通过（拒绝纸面抽象）
- Pitfall 1/4/11 回归（echo loop / stack leak / 双调 LLM）必须有 E2E 覆盖
- 用 browser-harness CDP 直连 Chrome — 用户硬性指令（memory feedback_e2e_browser_harness_only）
- 与 Phase 4 e2e_v2 模式一致 — Smoke / Standard / Full 三档运行
- Mattermost container fixture 启动真 MM server（mattermost/mattermost-team-edition image）— bot 真连真测

Output: 1 个 browser-harness reading doc + 1 个 Mattermost test client + 1 个 fixture 模块 + 1 个 conftest + 5 个 E2E spec + backend test API 扩展 + e2e_v2 README 更新。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04_5-bot-triggers/04_5-RESEARCH.md
@CLAUDE.md
@e2e_v2/conftest.py
@e2e_v2/README.md
@e2e_v2/specs/test_04_chain_sequential.py
@e2e_v2/helpers/api_client.py
@e2e_v2/helpers/im_mock_client.py
@.planning/phases/04-approval-chain-im/04-12-PLAN.md
@.planning/phases/04-approval-chain-im/04-12-SUMMARY.md
@backend/app/agent_builder/bot_dispatcher/dispatcher.py
@backend/app/agent_builder/services/bot_lifecycle.py
@plugins/bots/test_workflow_bot.yaml

<interfaces>
<!-- 本 plan 提供 E2E spec + Mattermost test client + container fixture。 -->

From e2e_v2/helpers/mattermost_client.py（本 plan 创建）:
```python
class MattermostTestClient:
    """通过 Mattermost API v4 操作 — admin login / create bot / send DM / get messages。

    用于 E2E spec 内 — 真连 Mattermost container（不 mock）。
    """

    def __init__(self, *, base_url: str, admin_token: str) -> None: ...

    async def login_as_admin(self) -> str: ...
    async def create_user(self, *, username: str, email: str, password: str) -> dict: ...
    async def create_bot(self, *, username: str, display_name: str) -> tuple[str, str]:
        """Returns (bot_user_id, bot_token)。"""
    async def create_channel(self, *, team_id: str, name: str) -> dict: ...
    async def get_or_create_dm(self, *, user_a: str, user_b: str) -> dict: ...
    async def post_message(self, *, channel_id: str, message: str, as_user_token: str) -> dict: ...
    async def get_channel_messages(self, *, channel_id: str, limit: int = 10) -> list[dict]: ...
    async def wait_for_bot_reply(
        self, *, channel_id: str, after_message_id: str, timeout: float = 30
    ) -> dict | None:
        """polling — 等待 bot 回帖（用于 E2E 断言）。"""
```

From e2e_v2/helpers/bot_test_fixtures.py（本 plan 创建）:
```python
@pytest.fixture(scope="session")
async def mm_container() -> AsyncIterator[dict]:
    """启动 mattermost/mattermost-team-edition Docker container。

    Returns: {'base_url': 'http://localhost:PORT', 'admin_token': '...', 'team_id': '...'}
    """


@pytest.fixture(scope="session")
async def bot_admin(mm_container) -> AsyncIterator[MattermostTestClient]:
    """Admin client — 用于 setup bot account + user account。"""


@pytest.fixture
async def fresh_bot_setup(bot_admin, mm_container) -> AsyncIterator[dict]:
    """每 test 创建：bot account + test_user + test channel。返回 bundle dict。"""
```

Backend test API extension（本 plan 扩展 e2e_v2 已有 test_helpers）:
```python
# backend/app/agent_builder/api/test_helpers.py（已存在 Phase 4-12 — 本 plan 扩展）
@router.get("/test/bot_audit_logs")
async def get_bot_audit_logs(
    workspace_id: UUID,
    bot_name: str | None = None,
    limit: int = 50,
) -> list[dict]:
    """E2E 断言 routed_via / outcome / latency_ms / llm_confidence。仅 ENABLE_TEST_API=1 挂载。"""
```
</interfaces>
</context>

<reference>
**browser-harness reading doc 必读（用户硬性指令 — memory feedback_e2e_browser_harness_only）**:
1. `/Users/admin/.claude/skills/browser-harness/README.md` 或 entry point
2. `/Users/admin/.claude/skills/browser-harness/SKILL.md`（如存在）
3. 既有用法参考: `e2e_v2/conftest.py` + `e2e_v2/helpers/browser_session.py`（如已有 Phase 4-12 留下的 helper）

**Phase 4 e2e_v2 模式参考（强制对照）**:
- `/Users/admin/ai/resume/interview/liuxin/agent-builder/.planning/phases/04-approval-chain-im/04-12-PLAN.md` — 完整 6 spec 模式
- `/Users/admin/ai/resume/interview/liuxin/agent-builder/.planning/phases/04-approval-chain-im/04-12-SUMMARY.md` — 执行经验
- `/Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2/specs/test_04_chain_sequential.py` — spec 结构参考

**Mattermost API 文档**:
- https://api.mattermost.com/ — REST API v4 参考
- mattermost/mattermost-team-edition Docker image — 单容器测试用

借鉴重点（reading doc 必含 4-6 条 takeaway）:
1. browser-harness CDP 调用模式（session 启动 / 浏览器操作 / 截图 / 等待元素）
2. Phase 4-12 spec 26 个的组织模式（与本 plan 5 spec 复用 fixture / helper / Smoke marker）
3. Mattermost container setup 与启动等待（health check / token 注入）
4. bot account 通过 Admin API 动态创建 / cleanup
5. E2E 等待 bot 回帖的 polling 策略（避免 race condition）
6. 与 Phase 4-12 spec 不冲突的 marker / fixture 命名

License: browser-harness 是 user-private skill；不入仓。
</reference>

<tasks>

<task type="auto">
  <name>Task 0: browser-harness reading doc gate（用户硬性指令）</name>
  <files>docs/reading-browser-harness-04_5-06-2026-05-18.md</files>
  <action>
    阅读 browser-harness skill 文档 + Phase 4-12 e2e_v2 模式后撰写阅读笔记。**必须先 commit 此 doc 才能进 Task 1**。

    必读文件:
    1. `/Users/admin/.claude/skills/browser-harness/SKILL.md` 或入口 README
    2. `/Users/admin/ai/resume/interview/liuxin/agent-builder/.planning/phases/04-approval-chain-im/04-12-PLAN.md`
    3. `/Users/admin/ai/resume/interview/liuxin/agent-builder/.planning/phases/04-approval-chain-im/04-12-SUMMARY.md`
    4. `/Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2/conftest.py`
    5. `/Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2/specs/test_04_chain_sequential.py`

    若 browser-harness skill 路径不存在，先用 `ls /Users/admin/.claude/skills/` 找路径；fallback 读 `~/.claude/CLAUDE.md` 中 browser-harness 提及。

    文档结构按 CLAUDE.md §2.7 模板（80+ 行）含：
    - 项目概述（browser-harness 是 Claude CDP skill / Phase 4-12 已用过）
    - 技术栈（CDP / Chrome DevTools Protocol / Mattermost API / testcontainers vs docker-py）
    - 架构要点（browser session lifecycle / Mattermost container startup / bot account 创建链路）
    - 可借鉴的设计模式（≥4 条 [文件:行号] — Phase 4-12 + skill docs）
    - 与本项目的关系（5 spec × ROADMAP 5 criteria 一一对应 / 共用 fixture 模式 / Pitfall 1/4/11 回归断言）
    - License attribution（browser-harness 是 private skill；不入仓）

    至少 80 行；commit message: `docs(04_5-06): add browser-harness E2E reading doc`。
  </action>
  <verify>
    <automated>test -f /Users/admin/ai/resume/interview/liuxin/agent-builder/docs/reading-browser-harness-04_5-06-2026-05-18.md && awk 'END{exit (NR>=80)?0:1}' /Users/admin/ai/resume/interview/liuxin/agent-builder/docs/reading-browser-harness-04_5-06-2026-05-18.md</automated>
  </verify>
  <done>reading doc ≥ 80 行；commit 早于 Task 1。</done>
</task>

<task type="auto">
  <name>Task 1: Mattermost test client + container fixture + backend test API 扩展</name>
  <files>
    e2e_v2/helpers/__init__.py
    e2e_v2/helpers/mattermost_client.py
    e2e_v2/helpers/bot_test_fixtures.py
    e2e_v2/specs/conftest_phase_4_5.py
  </files>
  <action>
    1. **`e2e_v2/helpers/mattermost_client.py`** — Mattermost API v4 客户端（httpx async）:
       - `MattermostTestClient` 类含 `login_as_admin / create_user / create_bot / create_channel / get_or_create_dm / post_message / get_channel_messages / wait_for_bot_reply` 8 方法
       - 所有方法用 httpx.AsyncClient + Authorization: Bearer
       - `wait_for_bot_reply` 用 polling（asyncio.sleep(0.5) + max 30s timeout）
       - 仅依赖 httpx + 标准库 — 不引入 mattermostautodriver（避免与 listener 共享 httpx patch state）

    2. **`e2e_v2/helpers/bot_test_fixtures.py`** — Mattermost container fixture:
       ```python
       """Phase 4.5 E2E fixtures — Mattermost container + bot setup。"""
       from __future__ import annotations
       import asyncio
       import os
       import subprocess
       import time
       from collections.abc import AsyncIterator

       import httpx
       import pytest_asyncio

       from .mattermost_client import MattermostTestClient


       MM_IMAGE = "mattermost/mattermost-team-edition:9.10"
       MM_HEALTH_TIMEOUT = 90.0


       @pytest_asyncio.fixture(scope="session")
       async def mm_container() -> AsyncIterator[dict]:
           """启动 Mattermost docker container（用 docker-py 或 subprocess docker run）。

           v1 简化：用 subprocess docker run + 等待 /api/v4/system/ping ready。
           Production: 推荐用 testcontainers-python 但 v1 用 subprocess 更可移植。
           """
           # 1. docker run -d -p 8068:8065 mattermost/mattermost-team-edition
           container_name = f"mm-e2e-{int(time.time())}"
           port = int(os.environ.get("MM_E2E_PORT", "8068"))
           env_vars = [
               "-e", "MM_SERVICESETTINGS_SITEURL=http://localhost:8065",
               "-e", "MM_SQLSETTINGS_DRIVERNAME=sqlite3",
               "-e", "MM_SQLSETTINGS_DATASOURCE=/mm_data/mattermost.db",
           ]
           subprocess.run(
               ["docker", "run", "-d", "--name", container_name, "-p", f"{port}:8065"] +
               env_vars + [MM_IMAGE],
               check=True,
           )

           base_url = f"http://localhost:{port}"
           # 等待 ready
           start = time.monotonic()
           while time.monotonic() - start < MM_HEALTH_TIMEOUT:
               try:
                   async with httpx.AsyncClient(timeout=5) as client:
                       resp = await client.get(f"{base_url}/api/v4/system/ping")
                       if resp.status_code == 200:
                           break
               except Exception:
                   await asyncio.sleep(2)
           else:
               subprocess.run(["docker", "rm", "-f", container_name], check=False)
               raise RuntimeError("Mattermost container did not start in 90s")

           # 创建 admin user
           # ... (省略 setup admin + team — 真实实现需对 MM API 调 init endpoint)
           admin_token = "TODO_setup_admin_and_get_token"
           team_id = "TODO_create_team_and_get_id"

           try:
               yield {
                   "base_url": base_url,
                   "admin_token": admin_token,
                   "team_id": team_id,
                   "container_name": container_name,
               }
           finally:
               subprocess.run(["docker", "rm", "-f", container_name], check=False)


       @pytest_asyncio.fixture(scope="session")
       async def bot_admin(mm_container) -> AsyncIterator[MattermostTestClient]:
           client = MattermostTestClient(
               base_url=mm_container["base_url"],
               admin_token=mm_container["admin_token"],
           )
           yield client


       @pytest_asyncio.fixture
       async def fresh_bot_setup(bot_admin, mm_container) -> AsyncIterator[dict]:
           """每 test 独立 bot + test_user + DM channel。"""
           bot_username = f"bot-{int(time.time())}"
           test_username = f"user-{int(time.time())}"
           bot_user_id, bot_token = await bot_admin.create_bot(
               username=bot_username, display_name="Test Bot"
           )
           test_user = await bot_admin.create_user(
               username=test_username,
               email=f"{test_username}@e2e.test",
               password="Test1234!@#$",
           )
           dm_channel = await bot_admin.get_or_create_dm(
               user_a=bot_user_id, user_b=test_user["id"]
           )
           yield {
               "bot_user_id": bot_user_id,
               "bot_token": bot_token,
               "bot_username": bot_username,
               "test_user_id": test_user["id"],
               "test_username": test_username,
               "dm_channel_id": dm_channel["id"],
           }
           # cleanup（容器 session 结束时 docker rm — 单 user 不需清）
       ```

    3. **`e2e_v2/specs/conftest_phase_4_5.py`** — Phase 4.5 共用 fixture:
       ```python
       """Phase 4.5 共用 fixture — 与 Phase 4 conftest 不冲突（不同 marker）。"""
       from __future__ import annotations
       import os
       import pytest

       from e2e_v2.helpers.bot_test_fixtures import (  # noqa: F401
           mm_container, bot_admin, fresh_bot_setup,
       )


       RUN_E2E = os.environ.get("RUN_E2E", "").strip() == "1"

       phase_4_5_e2e_required = pytest.mark.skipif(
           not RUN_E2E,
           reason="设置 RUN_E2E=1 才跑 Phase 4.5 E2E spec",
       )
       ```

    4. **`e2e_v2/helpers/__init__.py`** 已存在 — 仅追加导出 MattermostTestClient（不动既有 Phase 4 helper）

    5. **Backend test API 扩展** — 修改既有 `backend/app/agent_builder/api/test_helpers.py`（Phase 4-12 已挂载，本 plan 仅追加 endpoint）：
       ```python
       # 仅追加 — 不删既有
       @router.get("/test/bot_audit_logs")
       async def get_bot_audit_logs(
           workspace_id: UUID,
           bot_name: str | None = None,
           limit: int = 50,
           session: AsyncSession = Depends(get_session),
       ) -> list[dict]:
           stmt = select(BotAuditLog).where(BotAuditLog.workspace_id == workspace_id)
           if bot_name:
               stmt = stmt.where(BotAuditLog.bot_name == bot_name)
           stmt = stmt.order_by(BotAuditLog.created_at.desc()).limit(limit)
           result = await session.execute(stmt)
           rows = result.scalars().all()
           return [
               {
                   "id": r.id,
                   "outcome": r.outcome,
                   "routed_via": r.routed_via,
                   "cmd_name": r.cmd_name,
                   "latency_ms": r.latency_ms,
                   "llm_confidence": r.llm_confidence,
                   "created_at": r.created_at.isoformat(),
               }
               for r in rows
           ]
       ```

    **避坑**:
    - docker container 名加时间戳防多 test session 冲突
    - MM container 启动慢（30-90s）— health polling 必须 timeout 充足
    - bot_admin / mm_container 用 session scope 减少启动次数
    - fresh_bot_setup 用 function scope 让每 test 独立 bot（避免 state leak）
    - Mattermost admin setup 涉及 init API（首次访问需创建 system admin）— 实施时 RTFM `/api/v4/system/init`
    - test_helpers endpoint 仅当 `ENABLE_TEST_API=1` 时挂载（与 Phase 4-12 一致 — 不能进 production）

    commit messages（3 commit）:
    - `feat(04_5-06): add MattermostTestClient + container fixture for E2E`
    - `feat(04_5-06): add Phase 4.5 conftest with shared fixtures`
    - `feat(04_5-06): extend test_helpers with /api/test/bot_audit_logs endpoint`
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2 && python -c "from helpers.mattermost_client import MattermostTestClient; print('OK')"</automated>
  </verify>
  <done>MattermostTestClient 可 import；conftest_phase_4_5.py 文件存在；test_helpers 含 /api/test/bot_audit_logs endpoint。</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 1.5: 验证 browser-harness skill 可用（人工 checkpoint）</name>
  <files>(human checkpoint — 无文件修改)</files>
  <action>
    人工验证 browser-harness skill 安装情况、CDP Chrome 连接是否可用、Phase 4-12 已有 e2e_v2 模式 — 步骤：

    1. 确认 `/Users/admin/.claude/skills/browser-harness/` 目录存在且有 SKILL.md
    2. 运行 `pgrep -f "chrome.*--remote-debugging-port" || echo "no chrome running"` 检查 CDP Chrome 是否在跑
    3. 如果 Chrome 未跑，按 browser-harness skill 文档启动 CDP Chrome
    4. 确认 `RUN_E2E=1` 可激活既有 Phase 4 e2e_v2 specs（运行 `cd e2e_v2 && RUN_E2E=0 pytest specs/test_04_chain_sequential.py --collect-only -q` 应当看到 N skipped）
    5. Mattermost 容器拉镜像（首次）：`docker pull mattermost/mattermost-team-edition:9.10` 完成

    Resume signal: 输入 "approved" 继续，或描述 setup 问题（如 CDP Chrome 启动失败 / Mattermost image 拉取失败）。如果 setup 不通过，Plan 04_5-06 的 Smoke 默认 skip 仍可 commit（E2E 实际跑由后续标 RUN_E2E=1 启用）。
  </action>
  <verify>
    <automated>ls /Users/admin/.claude/skills/browser-harness/ 2>/dev/null | grep -q SKILL || echo "browser-harness skill not found"; docker images mattermost/mattermost-team-edition 2>/dev/null | grep -q 9.10 || echo "MM image not pulled (optional)"</automated>
  </verify>
  <done>用户回复 "approved" 或 "approved with smoke-only"；checkpoint 通过后才能进 Task 2-4。</done>
</task>

<task type="auto">
  <name>Task 2: E2E spec 1 — @bot help 收到帮助 + Pitfall 1 防 echo loop 回归</name>
  <files>
    e2e_v2/specs/test_phase_4_5_help.py
  </files>
  <action>
    实现 spec 1（ROADMAP Phase 4.5 #1 + R-IM-03 + Pitfall 1 回归）:

    ```python
    """spec 1: @bot help 收到帮助 + Pitfall 1 回归（防 echo loop）。

    ROADMAP Phase 4.5 success criteria #1: Mattermost 用户 @-mention bot → 收到响应
    R-IM-03: help 元数据驱动 — 按 user_role 过滤命令
    Pitfall 1: bot 自己消息不触发死循环（发 1 条 → 等 2s → channel 仅 1 条 bot 回复）
    """
    from __future__ import annotations
    import asyncio
    import pytest

    from e2e_v2.specs.conftest_phase_4_5 import phase_4_5_e2e_required

    pytestmark = phase_4_5_e2e_required


    @pytest.mark.asyncio
    async def test_at_mention_help_returns_command_list(
        fresh_bot_setup, bot_admin
    ):
        """ROADMAP Phase 4.5 #1: @-mention bot → 收到 help."""
        setup = fresh_bot_setup
        bot_username = setup["bot_username"]
        dm_channel_id = setup["dm_channel_id"]

        # 1. 用 test_user 身份发消息 "@bot help"
        # 注意：test_user 的 token 由 bot_admin.create_user 时返回 — 或单独 login
        user_token = await bot_admin._login_user(setup["test_username"], "Test1234!@#$")
        from e2e_v2.helpers.mattermost_client import MattermostTestClient
        client_as_user = MattermostTestClient(
            base_url=bot_admin.base_url, admin_token=user_token
        )
        sent = await client_as_user.post_message(
            channel_id=dm_channel_id,
            message=f"@{bot_username} help",
            as_user_token=user_token,
        )

        # 2. 等 bot 回复（max 30s polling）
        reply = await bot_admin.wait_for_bot_reply(
            channel_id=dm_channel_id,
            after_message_id=sent["id"],
            timeout=30,
        )

        assert reply is not None, "bot 未在 30s 内回复"
        assert "命令清单" in reply["message"] or "help" in reply["message"].lower()
        assert "start" in reply["message"], "help 必须列出 start 命令"

    @pytest.mark.asyncio
    async def test_no_echo_loop_after_help_reply(fresh_bot_setup, bot_admin):
        """Pitfall 1 回归: bot 回 help 后 channel 应只有 1 条 bot 消息（不是无限循环）。"""
        setup = fresh_bot_setup
        bot_username = setup["bot_username"]
        dm_channel_id = setup["dm_channel_id"]

        user_token = await bot_admin._login_user(setup["test_username"], "Test1234!@#$")
        from e2e_v2.helpers.mattermost_client import MattermostTestClient
        client_as_user = MattermostTestClient(
            base_url=bot_admin.base_url, admin_token=user_token
        )
        await client_as_user.post_message(
            channel_id=dm_channel_id,
            message=f"@{bot_username} help",
            as_user_token=user_token,
        )

        # 等 5s — 让 listener / bot 完整处理
        await asyncio.sleep(5)

        # 拉全部消息
        messages = await bot_admin.get_channel_messages(channel_id=dm_channel_id, limit=20)
        bot_messages = [m for m in messages if m["user_id"] == setup["bot_user_id"]]

        # bot 应仅回 1 条（不是 echo loop）
        assert len(bot_messages) <= 1, f"echo loop detected: bot replied {len(bot_messages)} times"
    ```

    **避坑**:
    - `_login_user` 是 MattermostTestClient 内部方法 — Task 1 实施时按真实 API 调（如有 client.login 方法）
    - wait_for_bot_reply 用 polling — 不要用固定 sleep（race condition）
    - test_no_echo_loop_after_help_reply 必须等 ≥ 5s（让任何潜在 echo loop 有机会触发）
    - 用 conftest 的 fresh_bot_setup fixture — 每 test 独立 bot 避免 state leak

    commit message: `test(04_5-06): add E2E spec 1 - @bot help + Pitfall 1 echo loop regression`
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2 && python -m pytest specs/test_phase_4_5_help.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>spec 1 collect-only 通过（即使 RUN_E2E=0 也能 collect — Smoke skip）；2 个 test function 被收集。</done>
</task>

<task type="auto">
  <name>Task 3: E2E spec 2 — DM @bot start <wf_id> + spec 3 LLM intent routing</name>
  <files>
    e2e_v2/specs/test_phase_4_5_dm_start_workflow.py
    e2e_v2/specs/test_phase_4_5_llm_intent_routes_to_status.py
  </files>
  <action>
    1. **`test_phase_4_5_dm_start_workflow.py`** — spec 2（ROADMAP #2 + R-IM-05 DM 触发）:
       - test_dm_start_command_triggers_workflow：DM "start <wf_id>" → 收 "✅ 已启动 workflow ..."
       - test_dm_does_not_require_at_mention：DM 无 @mention 也触发（R-IM-05 channel_type='D'）
       - test_audit_log_records_dispatch：调 `/api/test/bot_audit_logs` 断言 outcome='success' / routed_via='whitelist' / cmd_name='start'

    2. **`test_phase_4_5_llm_intent_routes_to_status.py`** — spec 3（ROADMAP #3 + R-IM-04 LLM intent + Pitfall 11 短路验证）:
       - test_natural_language_routes_to_status_via_llm：发 "帮我看看流程进度" → bot 回复含状态信息 → 断言 audit log routed_via='llm_intent' / llm_confidence ≥ 0.6
       - test_keyword_match_bypasses_llm_router：bot.yaml 加 keyword "查询进度" → 发该 keyword → 断言 audit log routed_via='keyword_self_apply' / llm_confidence is None（Pitfall 11 防双调 LLM）
       - test_low_confidence_falls_back_to_ai_qa：发模糊查询 → 断言 audit log routed_via='ai_qa'

    每个 spec 模式与 spec 1 一致：
    - 用 fresh_bot_setup fixture
    - post_message → wait_for_bot_reply → assert reply content
    - 通过 backend `/api/test/bot_audit_logs` API 断言路由字段

    **避坑**:
    - spec 3 需要 backend lifespan 实际启动 bot — 集成方式：
      - Option A: 用 ENABLE_TEST_API=1 + 自动 lifespan 启动 plugins/bots/test_workflow_bot.yaml
      - Option B: spec 内通过 API 触发 lifespan.startup（test-only endpoint）
      - 推荐 A（与 Phase 4-12 mock_im_providers 模式一致）
    - LLM intent test 真调 GLM API — 需 ZHIPUAI_API_KEY 在 env 内
    - Pitfall 11 test 通过 audit log llm_confidence is None 断言 LLM 未被调用（vs 双调时会有 confidence 值）

    commit messages（2 commit）:
    - `test(04_5-06): add E2E spec 2 - DM start workflow (R-IM-05 + audit verification)`
    - `test(04_5-06): add E2E spec 3 - LLM intent routing + Pitfall 11 short-circuit regression`
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2 && python -m pytest specs/test_phase_4_5_dm_start_workflow.py specs/test_phase_4_5_llm_intent_routes_to_status.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <done>spec 2 + 3 collect-only 通过；test_audit_log_records_dispatch + test_keyword_match_bypasses_llm_router 关键防护用例存在。</done>
</task>

<task type="auto">
  <name>Task 4: E2E spec 4 — 权限拒绝 + spec 5 help auto-generated（含 Pitfall 4 stack trace 回归）</name>
  <files>
    e2e_v2/specs/test_phase_4_5_permission_denied.py
    e2e_v2/specs/test_phase_4_5_help_auto_generated.py
  </files>
  <action>
    1. **`test_phase_4_5_permission_denied.py`** — spec 4（ROADMAP #4 + R-IM-06 + Pitfall 4 回归）:
       - test_viewer_cannot_call_admin_command：viewer 角色用户调 admin-only 命令 → 收 "❌" 友好提示 → 断言 audit log outcome='permission_denied'
       - test_unknown_user_reject_friendly：sender_name 不在 users 表 → 收 bot_config.identity.unknown_hint 友好提示
       - test_handler_exception_wrapped_no_stack_trace_leak (Pitfall 4 + N-IM-04 P0)：故意触发 handler raise Exception → 收 "❌ 内部错误（事件 ID: <id>）..." → 断言回复**不含** "Traceback" / "File \"..." / "AttributeError" 等 stack 关键词

    2. **`test_phase_4_5_help_auto_generated.py`** — spec 5（ROADMAP #5 + R-IM-03 元数据驱动）:
       - test_help_lists_all_visible_commands：admin 调 @bot help → 收到含 start / status / list / help 4 命令
       - test_help_filters_by_role：viewer 调 @bot help → 不含 admin-only 命令
       - test_adding_new_command_to_yaml_appears_in_help_after_reload：
         - setup 1: 用初始 bot.yaml 启动（3 命令）
         - 调 help 断言 3 命令
         - **runtime 不支持 reload**（v1 — R-IM-10 v2）— spec 改为：用 monkey-patch BotConfig 加新命令 → 调 help → 断言新命令在输出（验证 handle_help 不缓存）
       - test_help_includes_examples_when_configured：bot.yaml example_invocations 非空 → help 输出含 "示例：" 行

    **避坑**:
    - test_handler_exception_wrapped_no_stack_trace_leak 是 Pitfall 4 P0 关键回归 — 不要简化
    - 实施 handler 异常触发可通过 stub workflow_id "trigger_exception" 让 builtin handle_start 主动 raise（test helper）
    - test_unknown_user_reject_friendly 通过 setup 不创建 users 表对应行
    - help auto-generated test 不在 spec 内重启 listener（v1 不支持 reload）；用 in-process BotConfig 修改

    commit messages（2 commit）:
    - `test(04_5-06): add E2E spec 4 - permission denied + Pitfall 4 stack trace regression`
    - `test(04_5-06): add E2E spec 5 - help auto-generated (R-IM-03)`
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2 && python -m pytest specs/test_phase_4_5_permission_denied.py specs/test_phase_4_5_help_auto_generated.py --collect-only -q 2>&1 | tail -10</automated>
  </verify>
  <done>spec 4 + 5 collect-only 通过；test_handler_exception_wrapped_no_stack_trace_leak 关键 Pitfall 4 用例存在。</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: 全 5 spec Smoke + Standard + Mattermost container smoke 跑通</name>
  <files>(human checkpoint — 验证既有 spec 文件)</files>
  <action>
    人工验证 5 个 E2E spec + Mattermost container fixture + 全 Pitfall 1/4/11 回归。验证步骤：
    1. **Smoke 模式（默认 CI — 0s）**：
       ```bash
       cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2
       pytest specs/test_phase_4_5_*.py -v 2>&1 | tail -20
       ```
       期望：全部 ≥ 10 个 test 标记 `SKIPPED [reason='设置 RUN_E2E=1 才跑 Phase 4.5 E2E spec']`

    2. **Standard 模式（手动 — 实际跑）**：
       ```bash
       cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2
       export RUN_E2E=1
       export ENABLE_TEST_API=1
       export MM_E2E_PORT=8068
       # 假设 backend 已起 + plugins/bots/test_workflow_bot.yaml 已加载
       pytest specs/test_phase_4_5_help.py -v 2>&1 | tail -30
       ```
       期望：spec 1 至少 1 个 test pass（如 Mattermost container setup + bot.yaml lifespan 启动顺利）

    3. **Pitfall 1 回归（关键）**：
       ```bash
       pytest specs/test_phase_4_5_help.py::test_no_echo_loop_after_help_reply -v
       ```
       期望：pass（5s 后 bot 消息数 ≤ 1）

    4. **Pitfall 4 回归（关键 P0 安全）**：
       ```bash
       pytest specs/test_phase_4_5_permission_denied.py::test_handler_exception_wrapped_no_stack_trace_leak -v
       ```
       期望：pass（bot 回复不含 "Traceback" / "File \"..."）

    5. **Pitfall 11 回归（成本）**：
       ```bash
       pytest specs/test_phase_4_5_llm_intent_routes_to_status.py::test_keyword_match_bypasses_llm_router -v
       ```
       期望：pass（audit log routed_via='keyword_self_apply' / llm_confidence is None）

    6. **regression 验证**：
       ```bash
       cd /Users/admin/ai/resume/interview/liuxin/agent-builder/backend
       pytest tests/platforms/ tests/notification/ tests/agent_builder/bot_dispatcher/ -x -q
       ```
       期望：5.A 162 + Phase 4 81 + Plan 01-05 130+ = 全绿

    Resume signal: 输入 "approved" 通过；或描述问题（如 Mattermost container setup 失败 / lifespan 启动失败 / LLM 调用失败）；或 "approved with smoke-only"（CI 仅 collect smoke / Standard 由实际部署后再跑）。

    Smoke 通过 + collect-only 全 5 spec 枚举成功即可视为 plan complete（Standard 测试用例存在即可，runtime 跑通由后续部署阶段验证）。
  </action>
  <verify>
    <automated>cd /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2 && python -m pytest specs/test_phase_4_5_*.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <done>5 spec 全 collect 通过 + 用户回复 "approved" 或 "approved with smoke-only"；至少 Pitfall 1/4/11 三关键回归用例存在（即使 Standard runtime 由后续部署验证）。</done>
</task>

<task type="auto">
  <name>Task 6: README + e2e_v2 文档更新</name>
  <files>
    e2e_v2/README.md
  </files>
  <action>
    在既有 `e2e_v2/README.md` 末尾追加 Phase 4.5 spec 说明（不删既有 Phase 4 章节）：

    ```markdown
    ## Phase 4.5 E2E specs（IM Bot 入站 dispatcher）

    | Spec | ROADMAP # | 验证内容 | Pitfall 回归 |
    |------|----------|---------|------------|
    | test_phase_4_5_help.py | #1 | @-mention bot help / R-IM-03 元数据 | Pitfall 1 echo loop |
    | test_phase_4_5_dm_start_workflow.py | #2 | DM 启动 workflow / R-IM-05 触发 | — |
    | test_phase_4_5_llm_intent_routes_to_status.py | #3 | LLM intent 路由 / R-IM-04 | Pitfall 11 双调短路 |
    | test_phase_4_5_permission_denied.py | #4 | RBAC / R-IM-06 | Pitfall 4 stack trace leak |
    | test_phase_4_5_help_auto_generated.py | #5 | help 元数据 / R-IM-03 | — |

    ### 运行

    **Smoke (默认 0s)**: `pytest specs/test_phase_4_5_*.py -v` (all skipped — RUN_E2E=1 才跑)

    **Standard (Mattermost container + lifespan)**:
    ```bash
    docker pull mattermost/mattermost-team-edition:9.10
    export RUN_E2E=1 ENABLE_TEST_API=1 MM_E2E_PORT=8068
    pytest specs/test_phase_4_5_*.py -v
    ```

    ### 依赖

    - Mattermost container: mattermost/mattermost-team-edition:9.10（90s 启动）
    - Backend: lifespan 必须加载 plugins/bots/test_workflow_bot.yaml
    - LLM: ZHIPUAI_API_KEY 在 env（spec 3 真调 GLM）
    - Test API: ENABLE_TEST_API=1 暴露 /api/test/bot_audit_logs
    ```

    commit message: `docs(04_5-06): update e2e_v2 README with Phase 4.5 specs`
  </action>
  <verify>
    <automated>grep -q "Phase 4.5 E2E specs" /Users/admin/ai/resume/interview/liuxin/agent-builder/e2e_v2/README.md</automated>
  </verify>
  <done>README 含 Phase 4.5 章节 + 5 spec 表格 + 运行说明。</done>
</task>

</tasks>

<verification>
**phase-local checks (collect-only Smoke)**:
- `cd e2e_v2 && pytest specs/test_phase_4_5_*.py --collect-only -q` 5 spec ≥ 10 test 全部成功 collect
- `cd e2e_v2 && pytest specs/test_phase_4_5_*.py -v 2>&1 | grep -c SKIPPED` ≥ 10（Smoke 默认 skip）

**regression**:
- `cd backend && pytest tests/platforms/ tests/notification/ tests/agent_builder/bot_dispatcher/ -x` 0 fail
- `cd e2e_v2 && pytest specs/test_04_*.py --collect-only -q` Phase 4 既有 26 specs 0 影响（仅 collect）
- 累积 Plan 01-06 测试 ≥ 140 全绿（不含 Standard runtime — Standard 由部署验证）

**reading doc gate**:
- `git log --oneline -10` 第一个 `docs(04_5-06)` commit 早于任何 `test(04_5-06)` / `feat(04_5-06)` commit

**Standard runtime check（手动 — checkpoint）**:
- Task 5 checkpoint 通过：至少 spec 1 (help) + Pitfall 1/4/11 三个关键回归用例真跑 pass
</verification>

<success_criteria>
1. **5 spec 覆盖 ROADMAP**: 每 spec 头注释明示对应 ROADMAP Phase 4.5 success criterion #N
2. **browser-harness CDP only**: 严守用户硬性指令 — 禁 Playwright / webapp-testing
3. **Pitfall 1/4/11 回归**: 三关键防护用例覆盖（echo loop / stack trace leak / LLM 双调）
4. **Mattermost container 集成**: testcontainers 或 subprocess docker run + health polling
5. **三档运行模式**: Smoke (默认 skip) / Standard (RUN_E2E=1) / Full
6. **backend test API 扩展**: /api/test/bot_audit_logs 仅 ENABLE_TEST_API=1 挂载
7. **regression**: 5.A + Phase 4 + Plan 01-05 全绿
8. **reading doc gate**: docs commit 早于 feat/test commit
9. **README 更新**: e2e_v2/README.md 含 Phase 4.5 章节
10. **autonomous=false**: 含 2 个 checkpoint（Task 1.5 setup verify + Task 5 spec smoke verify）
</success_criteria>

<output>
After completion, create `.planning/phases/04_5-bot-triggers/04_5-06-SUMMARY.md` 含:
- browser-harness skill 借鉴点（reading doc 关键 takeaway 4-6 条）
- 5 spec × ROADMAP 5 criteria 一一对应表
- Pitfall 1/4/11 三关键回归用例（spec / 断言点）
- Mattermost container fixture 启动流程 + 优化点
- backend test API 扩展（/api/test/bot_audit_logs）
- 三档运行模式约定（与 Phase 4-12 一致）
- 与 Phase 5.E（飞书/企微/钉钉/Slack 入站）的接口契约：本 plan 验证 abstraction OK，5.E 仅替换 listener
- 测试矩阵（5 spec × 平均 3 test = 15 E2E + 容器 smoke 1）
</output>
</content>
</invoke>