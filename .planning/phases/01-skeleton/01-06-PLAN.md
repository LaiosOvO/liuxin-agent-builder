---
phase: 01-skeleton
plan: 06
type: execute
wave: 4
depends_on: ["01-02", "01-03", "01-04", "01-05"]
files_modified:
  - e2e/setup-flow.spec.ts
  - e2e/invite-flow.spec.ts
  - e2e/login-flow.spec.ts
  - e2e/rbac-isolation.spec.ts
  - e2e/hmac-startup.spec.ts
  - e2e/docker-compose-up.spec.ts
  - e2e/safe-links-future-proof.spec.ts
  - e2e/helpers/test-data.ts
  - e2e/helpers/api-client.ts
  - e2e/helpers/mailhog-client.ts (邮件捕获辅助 - 使用 mailhog 替代真 SMTP)
  - e2e/helpers/db-reset.ts
  - e2e/playwright.config.ts (扩展：projects + retries + webServer)
  - e2e/global-setup.ts
  - e2e/global-teardown.ts
  - docker-compose.test.yml (override：mailhog 替代 SMTP / 测试数据库)
  - scripts/e2e_run.sh (一键拉起测试环境 + 跑 E2E)
  - .github/workflows/ci.yml (CI 集成 - 可选)
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-06
  - NET-01
  - NET-02
  - NET-03
  - NET-04
  - DEPL-01
  - DEPL-02
must_haves:
  truths:
    - "E2E 用例覆盖 ROADMAP.md Phase 1 全部 5 条 success criteria"
    - "docker-compose up + alembic upgrade 一条命令把测试环境完整起来"
    - "MailHog 捕获邮箱验证邮件 + 邀请邮件，E2E 测试能通过解析邮件内链接完成全流程"
    - "双 workspace 互访 RBAC 边界 4 个用例全过"
    - "HMAC_SECRET < 32 字节启动失败的 E2E 用例从 process exit code 验证"
    - "Safe Links 未来防护占位用例（Phase 1 阶段 HITL 还未做，标 test.fixme 留 Phase 3 启用）"
  artifacts:
    - path: "e2e/setup-flow.spec.ts"
      provides: "ROADMAP success criterion 3 + 部分 1：docker-compose up 后浏览器看到 setup → 创建 → dashboard"
      contains: "setup-wizard"
    - path: "e2e/invite-flow.spec.ts"
      provides: "邀请链路全流程：admin 发邀请 → 邮件解析 → 接受 → 自动登录"
    - path: "e2e/rbac-isolation.spec.ts"
      provides: "ROADMAP success criterion 1+2：editor/viewer 边界 + 双 workspace 互访 403"
    - path: "e2e/hmac-startup.spec.ts"
      provides: "ROADMAP success criterion 5：HMAC_SECRET 太短启动失败"
    - path: "e2e/docker-compose-up.spec.ts"
      provides: "ROADMAP success criterion 3+4：6-service 全 healthy + nginx 路径扫描"
    - path: "e2e/safe-links-future-proof.spec.ts"
      provides: "Phase 3 HITL Safe Links 防护用例占位（test.fixme，Phase 3 移除 fixme）"
    - path: "scripts/e2e_run.sh"
      provides: "一条命令：docker-compose -f docker-compose.yml -f docker-compose.test.yml up -d && 等待健康 && alembic upgrade head && playwright test"
  key_links:
    - from: "e2e/global-setup.ts"
      to: "docker-compose.test.yml"
      via: "spawn docker-compose up"
      pattern: "docker compose"
    - from: "e2e/helpers/mailhog-client.ts"
      to: "MailHog API"
      via: "http://mailhog:8025/api/v2/messages"
      pattern: "/api/v2/messages"
    - from: "e2e/global-teardown.ts"
      to: "scripts/db_reset"
      via: "alembic downgrade base + upgrade head"
      pattern: "alembic"
---

<objective>
**Phase 1 集成验收。** 跑完本 plan 即 Phase 1 完成。

用 webapp-testing（Playwright）从浏览器视角端到端验证 ROADMAP.md Phase 1 5 条 success criteria：
1. 注册 + 登录 + 看到自己 workspace 内容（看不到别人的）
2. RBAC 生效（editor 编辑 / viewer 只读 / admin 管理）
3. docker-compose up 一键启动 + 浏览器打开画布页
4. nginx 仅放行 3 条路径（其他 403）
5. HMAC_SECRET < 32 字节启动失败

Purpose: CLAUDE.md 2.2 全流程测试 + E2E 第一公民；本 plan 是 Phase 1 验收门槛，过则 Phase 1 完成。

Output: 6 个 spec 文件 + helpers + docker-compose.test.yml + 一键脚本 + 可选 CI workflow。
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/01-skeleton/01-01-SUMMARY.md
@.planning/phases/01-skeleton/01-02-SUMMARY.md
@.planning/phases/01-skeleton/01-03-SUMMARY.md
@.planning/phases/01-skeleton/01-04-SUMMARY.md
@.planning/phases/01-skeleton/01-05-SUMMARY.md

<webapp_testing_skill_usage>
本 plan 必须使用 Skill `webapp-testing` 编写 Playwright 测试：
- E2E 用例放 `e2e/` 目录
- Playwright config 见 Plan 01 的 e2e/playwright.config.ts，本 plan 扩展之
- Mailhog 替代真 SMTP 让测试可重复（捕获邮件 + 解析链接）
</webapp_testing_skill_usage>

<test_environment_topology>
**docker-compose.test.yml（override base docker-compose.yml）：**
```yaml
services:
  postgres:
    environment:
      POSTGRES_DB: agent_builder_test
  mailhog:
    image: mailhog/mailhog
    ports:
      - "1025:1025"  # SMTP
      - "8025:8025"  # Web UI / API
  api:
    environment:
      SMTP_HOST: mailhog
      SMTP_PORT: 1025
      SMTP_USERNAME: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: "noreply@agent-builder.test"
      POSTGRES_DSN: postgresql+asyncpg://postgres:postgres@postgres:5432/agent_builder_test
      ENVIRONMENT: test
  worker:
    environment:
      POSTGRES_DSN: postgresql+asyncpg://postgres:postgres@postgres:5432/agent_builder_test
```
</test_environment_topology>

<mailhog_client_spec>
```typescript
// e2e/helpers/mailhog-client.ts
export async function getLatestEmail(toEmail: string, timeout=10000) {
  const start = Date.now();
  while (Date.now() - start < timeout) {
    const r = await fetch('http://localhost:8025/api/v2/messages');
    const data = await r.json();
    const found = data.items.find((m: any) => m.To.some((t: any) => `${t.Mailbox}@${t.Domain}` === toEmail));
    if (found) return found;
    await new Promise(r => setTimeout(r, 300));
  }
  throw new Error(`No email to ${toEmail} within ${timeout}ms`);
}
export function extractLink(email: any, hrefContains: string): string {
  const html = email.Content.Body;
  const m = html.match(new RegExp(`href="([^"]*${hrefContains}[^"]*)"`));
  if (!m) throw new Error(`No link with ${hrefContains}`);
  return m[1];
}
export async function purgeAllEmails() { await fetch('http://localhost:8025/api/v1/messages', { method: 'DELETE' }); }
```
</mailhog_client_spec>

<roadmap_success_criteria_coverage_table>
| ROADMAP Success Criteria | Covered By Spec | Test Cases |
|---|---|---|
| 1. 注册登录 + workspace 隔离 | setup-flow.spec / invite-flow.spec / rbac-isolation.spec | 注册流程 + 双 workspace 互访 403 |
| 2. RBAC（editor/viewer/admin） | rbac-isolation.spec | 3 role × 多 endpoint 矩阵 |
| 3. docker-compose up + 画布页 | docker-compose-up.spec / setup-flow.spec | 6 service healthy + 浏览器打开 dashboard |
| 4. nginx 仅 3 条路径 | docker-compose-up.spec (合并 Plan 03 nginx_path_scan.spec) | 50+ 路径 403 + 3 路径放行 |
| 5. HMAC_SECRET < 32 字节启动失败 | hmac-startup.spec | docker-compose run --rm api 退出码非 0 + 日志匹配中文错误 |
</roadmap_success_criteria_coverage_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 测试环境 (docker-compose.test.yml + helpers + global-setup/teardown)</name>
  <files>
    - docker-compose.test.yml
    - e2e/global-setup.ts
    - e2e/global-teardown.ts
    - e2e/helpers/test-data.ts (test user / workspace factories)
    - e2e/helpers/api-client.ts (E2E 直调 backend，绕过 UI 的 setup helpers)
    - e2e/helpers/mailhog-client.ts (按 spec)
    - e2e/helpers/db-reset.ts
    - e2e/playwright.config.ts (扩展)
    - scripts/e2e_run.sh
  </files>
  <action>
    1. **docker-compose.test.yml**：按 <test_environment_topology> 写；mailhog service 加 healthcheck（curl localhost:8025/api/v1/messages）。
    
    2. **playwright.config.ts 扩展**：
       - `globalSetup: './global-setup.ts'`、`globalTeardown: './global-teardown.ts'`
       - `webServer` 改成 `{ command: 'bash ../scripts/e2e_run.sh', port: 8080, timeout: 180000, reuseExistingServer: !!process.env.CI }` — 但建议改为 `globalSetup` 处理 docker-compose，避免 playwright 自身 spawn 引入 race。
       - `projects: [{ name: 'chromium', use: devices['Desktop Chrome'] }, { name: 'webkit', use: devices['Desktop Safari'] }]`
       - `retries: process.env.CI ? 2 : 0`
       - `expect: { timeout: 5000 }`、`use.actionTimeout: 10000`
       - `reporter: [['list'], ['html', { open: 'never' }]]`
    
    3. **global-setup.ts**：
       - 用 child_process spawn `docker compose -f docker-compose.yml -f docker-compose.test.yml up -d`
       - 等待 6 service healthcheck pass（poll docker compose ps，最长 120s）
       - 跑 `docker compose exec -T api alembic upgrade head` 确保 DB schema
       - 调 mailhog purgeAllEmails
       - 调 db-reset.ts truncate 测试表
    
    4. **global-teardown.ts**：
       - 输出 docker compose logs 摘要到 e2e/logs/
       - `docker compose -f docker-compose.yml -f docker-compose.test.yml down -v`（含 volume 清理）
       - 跳过：如果 `process.env.E2E_KEEP_RUNNING=1`
    
    5. **helpers/test-data.ts**：
       - `async function createSuperAdminAndWorkspace(name='Test WS'): Promise<{cookie, user, workspace}>` — 用 fetch 直调 backend setup/initialize（绕过 UI 加速）
       - `async function inviteAndAccept(adminCookie, email, role, password, displayName): Promise<{cookie, user}>` — 直调 API + 解析邮件 + 接受
       - `async function loginAs(email, password): Promise<{cookie}>`
       - 工厂保证测试 isolation：每个 spec 使用 unique workspace + unique emails（uuid 后缀）
    
    6. **helpers/api-client.ts**：fetch wrapper 用于 E2E 直调 backend，避免每个测试都 UI driven（UI driven 慢 + 脆弱）
    
    7. **helpers/db-reset.ts**：
       - `async function resetTestDb(): Promise<void>` — TRUNCATE workspaces/users/roles 之外的全部业务表 RESTART IDENTITY CASCADE；保留 4 个 role 种子；spec.afterEach 调用
    
    8. **scripts/e2e_run.sh**：
       ```bash
       set -euo pipefail
       docker compose -f docker-compose.yml -f docker-compose.test.yml up -d
       # wait for health
       for i in {1..60}; do
         if docker compose ps --format json | jq -e '.[] | select(.Health != "" and .Health != "healthy")' >/dev/null 2>&1; then sleep 2; else break; fi
       done
       docker compose exec -T api alembic upgrade head
       cd e2e && npx playwright test "$@"
       ```
       chmod +x。
  </action>
  <verify>
    <automated>
    test -f docker-compose.test.yml && test -f scripts/e2e_run.sh && test -x scripts/e2e_run.sh &&
    cd e2e && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    docker-compose.test.yml 含 mailhog；global-setup/teardown 编译通过；helpers 全部就位；scripts/e2e_run.sh 可执行
  </done>
</task>

<task type="auto">
  <name>Task 2: 6 个 E2E spec（覆盖 ROADMAP 5 条 success criteria + Safe Links 占位）</name>
  <files>
    - e2e/setup-flow.spec.ts
    - e2e/invite-flow.spec.ts
    - e2e/login-flow.spec.ts
    - e2e/rbac-isolation.spec.ts
    - e2e/hmac-startup.spec.ts
    - e2e/docker-compose-up.spec.ts
    - e2e/safe-links-future-proof.spec.ts
  </files>
  <action>
    **setup-flow.spec.ts**（ROADMAP criterion 3 + 部分 1）：
    - test "首次启动重定向到 /setup"：fresh DB → goto / → URL endsWith /setup
    - test "完整 setup wizard 流程"：填表 → submit → URL endsWith /dashboard → assert 显示 workspace 名
    - test "初始化后 /setup 跳 /"：再 visit /setup → 跳 /dashboard
    - test "弱密码前端拒绝"：输入 "abc" → 显示中文错误
    - test "重复 setup 后端拒绝"：mock api 返回 409 → toast 错误
    
    **login-flow.spec.ts**（criterion 1）：
    - test "登录成功跳 dashboard"
    - test "密码错误 401 + 中文错误"
    - test "邮箱未验证 403 + 提示去邮箱"
    - test "用户被禁用 403"
    - test "限频 429 + 中文提示"
    - test "/login?verified=1 显示成功 toast"
    - test "logout 清 cookie + 跳 login"
    
    **invite-flow.spec.ts**（criterion 1+2 邮件链路）：
    - test "admin 发邀请 → 邮件捕获 → 接受 → 自动登录"
      - 用 createSuperAdminAndWorkspace
      - UI 操作：dashboard/members → 发邀请 modal → 输入 email/role → submit
      - mailhog 等邮件 → extractLink hrefContains='/invite?token='
      - 新浏览器 context 打开链接 → 填密码 → submit → URL 跳 /dashboard
      - assert 新用户的 me.role_in_current_workspace = 'editor' (or 邀请的角色)
    - test "邀请链接已使用 → 显示失效页"
    - test "邀请链接过期 → 显示失效页"（用 helpers 直调 DB 把 expires_at 改成过去）
    - test "editor 看不见发邀请按钮（RoleGate）"
    
    **rbac-isolation.spec.ts**（criterion 1+2 多租户隔离）：
    - test "双 workspace 互访 dashboard/members 返回 403/空集"
      - 创建 ws_a + alice(admin in ws_a)；ws_b + bob(admin in ws_b)
      - alice 登录 → 直接构造 URL 访问 ws_b 的 members 页（如有）→ 跳 dashboard 或显示 403
      - alice 用 api/me 切换 workspace 到 ws_b → 403
    - test "editor 不能发邀请（UI 软隔离 + API 硬隔离）"
      - editor 登录 → dashboard/members 页面无发邀请按钮（RoleGate）
      - editor 用 fetch 直接 POST /api/invites → 403
    - test "viewer 不能编辑工作流（占位）"
      - viewer 登录 → /dashboard/canvas → 占位画布无编辑按钮（v1 占位即可）
    - test "external 无法登录"（Phase 1 external 还未通过 token 登录，此用例 fixme 留 Phase 3 启用）：`test.fixme(true, 'Phase 3 引入 external token 登录')`
    
    **hmac-startup.spec.ts**（criterion 5）：
    - test "HMAC_SECRET < 32 字节时 api 容器启动失败"
      - 用 child_process exec `docker compose -f docker-compose.yml -f docker-compose.test.yml run --rm -e HMAC_SECRET=too_short api python -c "from backend.app.main import app"`
      - assert exit code != 0
      - assert stderr 含 "HMAC_SECRET 长度不足"
    - test "JWT_SECRET < 32 字节同样失败"
    - test "缺少 POSTGRES_DSN 启动失败"
    - test "全部 secrets ≥ 32 字节启动成功"
    
    **docker-compose-up.spec.ts**（criterion 3+4）：
    - test "6 service 全 healthy"：docker compose ps 解析 Health 状态
    - test "nginx 公网 80 仅放行 3 条路径"：复用 Plan 03 path_scan helpers，遍历 50+ 路径
    - test "nginx 内网 8080 能访问 /"：返回 200 含 agent-builder 字样
    - test "nginx 内网 8080 能访问 /api/health"：返回 200 {status:ok}
    - test "nginx 不暴露版本号"：response.headers.server 不含 nginx/X.Y
    
    **safe-links-future-proof.spec.ts**（Phase 3 占位）：
    - `test.fixme(true, 'Phase 3 引入 HITL token；此用例 Phase 3 启用')`
    - 内容写好但 fixme 跳过：UA=MICROSOFT_DEFENDER_BOT 或 OUTLOOK_SAFE_LINKS_BOT 触发 GET /hitl/page/<token>；assert response.status=200 且 DB hitl_tokens.used_at IS NULL（**Phase 1 没 hitl_tokens 表**，所以 Phase 3 Plan 写实际逻辑时再启用）
    - **目的**：现在写好骨架文件，Phase 3 只需移除 fixme 即可激活
  </action>
  <verify>
    <automated>
    bash scripts/e2e_run.sh setup-flow.spec.ts login-flow.spec.ts invite-flow.spec.ts rbac-isolation.spec.ts hmac-startup.spec.ts docker-compose-up.spec.ts --reporter=list
    </automated>
  </verify>
  <done>
    6 个 spec 全过（safe-links 占位 fixme 不计入 PASS）；所有 ROADMAP 5 条 success criteria 都有 E2E 覆盖；执行时间总和 ≤ 10 分钟
  </done>
</task>

<task type="auto">
  <name>Task 3: 整合 docs + 更新 STATE.md + ROADMAP 标记 + CI workflow (可选)</name>
  <files>
    - .planning/STATE.md (修改：Phase 1 完成更新)
    - .planning/ROADMAP.md (修改：Phase 1 标 [x])
    - README.md (新建或更新：含 quickstart + e2e 跑法)
    - docs/development.md (开发者快速上手)
    - docs/testing.md (三层测试如何跑)
    - .github/workflows/ci.yml (CI 集成，可选)
  </files>
  <action>
    1. **README.md**（中文，简洁）：
       - 项目一句话（CLAUDE.md 1 节抄）
       - Quickstart：`cp .env.example .env && docker compose up -d && open http://localhost:8080`（首次访问跳 setup）
       - 三层测试：`pytest backend/` / `cd web && npm test` / `bash scripts/e2e_run.sh`
       - 文档链接：docs/development.md, docs/testing.md
    
    2. **docs/development.md**：
       - 环境要求（Python 3.11+, Node 22+, Docker 25+）
       - 本地开发：docker-compose 模式 vs 本地裸跑模式
       - alembic 操作（upgrade / downgrade / autogenerate）
       - 调试技巧（pyspy / chrome devtools）
    
    3. **docs/testing.md**：
       - 三层测试矩阵
       - 各层运行方式
       - 调试失败用例
       - mailhog UI 访问（http://localhost:8025）
    
    4. **.github/workflows/ci.yml**（可选；项目还未推 GitHub 时可后置）：
       - on: pull_request + push to main
       - job: lint（ruff + biome / eslint）
       - job: backend tests（pytest with testcontainers - 需 docker-in-docker）
       - job: frontend tests（npm test）
       - job: e2e（docker compose up + playwright）
       - cache pip / npm 加速
    
    5. **更新 STATE.md**：
       - 当前 Phase: Phase 1 → Done
       - Progress: 100%
       - Last activity: "Phase 1 E2E 验收通过"
       - 加 Decision: "Phase 1 完成；Plan 1-6 全部交付"
    
    6. **更新 ROADMAP.md**：
       - `- [ ] **Phase 1: Skeleton + 账号体系**` → `- [x]`
       - Phase 1 Plans 列表填齐：`- [x] 01-01-PLAN.md — Fork + docker-compose + 测试基线`、`01-02 — DB schema + ScopedQuery`、`01-03 — nginx + 启动校验 + 限频`、`01-04 — Auth API + Setup + Invite`、`01-05 — 前端 Next.js`、`01-06 — E2E 验收`
       - 进度表 Phase 1 行：Plans 6/6 / Status Complete / Completed 日期
  </action>
  <verify>
    <automated>
    test -f README.md && test -f docs/development.md && test -f docs/testing.md &&
    grep -q "\[x\] \*\*Phase 1" .planning/ROADMAP.md &&
    grep -q "Phase 1 → Done\|Phase 1.*Complete\|Phase 1 完成" .planning/STATE.md
    </automated>
  </verify>
  <done>
    README + 2 个 docs 就位；ROADMAP Phase 1 标 [x] + Plan 列表填齐；STATE.md 更新 Phase 1 完成状态
  </done>
</task>

</tasks>

<verification>
1. `bash scripts/e2e_run.sh` 一条命令把 6 个 E2E spec 全过
2. ROADMAP.md Phase 1 5 条 success criteria 各自有对应 E2E 用例（验收矩阵在 SUMMARY 列）
3. MailHog 捕获邮件流程可重复 + 解析链接精确
4. 双 workspace 互访 4 个 RBAC 用例全过
5. HMAC startup 失败用例从 exit code 验证（不依赖 stderr 解析）
6. docker-compose.yml + .test.yml 组合配置无冲突
7. 总 E2E 执行时间 ≤ 10 分钟
</verification>

<success_criteria>
1. **Phase 1 ROADMAP 5 条 success criteria 全部 E2E 覆盖且通过**
2. 三层测试矩阵就位（pytest + vitest + playwright）+ 一键脚本
3. 文档 README + development + testing 三件套就位
4. Plan 1-6 全部 Summary 已写；STATE.md / ROADMAP.md 反映 Phase 1 完成
5. Phase 2 可以 /gsd:discuss-phase 2 启动
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-06-SUMMARY.md` documenting:
- E2E 用例与 ROADMAP success criteria 映射表（完整版）
- Playwright 执行时间（每 spec 几秒）
- MailHog 邮件捕获范例（截屏或 raw text）
- 双 workspace 隔离测试输出
- HMAC startup 失败 exit code + stderr 截图
- README quickstart 步骤验证（实际跑过）
- Phase 1 整体验收清单（已完成 / 未完成）
</output>
