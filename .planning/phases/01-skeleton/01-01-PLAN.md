---
phase: 01-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/Dockerfile
  - web/package.json
  - web/Dockerfile
  - docker-compose.yml
  - .env.example
  - .gitignore
  - nginx/Dockerfile
  - nginx/nginx.conf
  - scripts/bootstrap_fork.sh
  - tests/conftest.py
  - tests/test_smoke.py
  - web/tests/smoke.spec.ts
  - e2e/playwright.config.ts
  - e2e/smoke.spec.ts
  - .planning/phases/01-skeleton/FORK_AUDIT.md
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
must_haves:
  truths:
    - "docker-compose up 后所有 6 个 service (api / worker / web / postgres / redis / nginx) 都进入 healthy"
    - "浏览器访问 http://localhost:8080 看到改名后的 agent-builder 落地页（不是 flock）"
    - ".env.example 包含 Phase 1 所有必需环境变量并附中文说明，且 .env 在 .gitignore 中"
    - "pytest + vitest + playwright 三套测试运行器在仓库根目录用一条命令各自跑通至少一个 smoke 用例"
    - "ChannelLog 中 Pydantic v1/v2 状态、Next.js 现行版本、FastAPI 版本写入 FORK_AUDIT.md"
  artifacts:
    - path: "backend/"
      provides: "fork 来自 Onelevenvy/flock 的后端目录树（保留原结构）"
      contains: "main.py / models / api"
    - path: "web/"
      provides: "fork 来自 Onelevenvy/flock 的前端目录树（已 rebrand 为 agent-builder）"
      contains: "package.json with name agent-builder-web"
    - path: "docker-compose.yml"
      provides: "6 个 service: api / worker / web / postgres / redis / nginx"
      min_lines: 60
    - path: ".env.example"
      provides: "所有 Phase 1 必需环境变量的占位符模板"
      contains: "HMAC_SECRET=CHANGE_ME_AT_LEAST_32_CHARS"
    - path: ".planning/phases/01-skeleton/FORK_AUDIT.md"
      provides: "flock 仓库快照盘点：Pydantic 版本 / Next.js 版本 / FastAPI 版本 / 现有 NodeRegistry 列表"
      min_lines: 40
    - path: "e2e/playwright.config.ts"
      provides: "Playwright E2E 测试基线配置（base URL=http://localhost:8080）"
    - path: "tests/conftest.py"
      provides: "pytest fixtures: testcontainers-postgres / redis async client / FastAPI ASGI client"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build context"
      pattern: "build:.*backend"
    - from: "docker-compose.yml"
      to: "web/Dockerfile"
      via: "build context"
      pattern: "build:.*web"
    - from: "docker-compose.yml"
      to: "nginx/nginx.conf"
      via: "volume mount"
      pattern: "nginx.conf"
---

<objective>
Fork Onelevenvy/flock → 改造成 agent-builder 工程底座 → 完成品牌重命名 → 落地 docker-compose 6-service 一键启动 → 建立三层测试基线（pytest / vitest / playwright）。

Purpose: 这是 Phase 1 所有其他 plan 的前置依赖。没有可运行的工程底座，后续的 DB / Auth / nginx / 前端 / E2E 都无从谈起。本 plan 完成后任何下游 plan 都能 git clone → docker-compose up → 开发。

Output:
- 改名完成的 backend/ + web/ 目录树（fork 自 flock）
- docker-compose.yml + 各 service Dockerfile
- .env.example + .gitignore 完整
- 三层测试 runner 各跑通一个 smoke
- FORK_AUDIT.md 记录 flock 当前版本与改造点
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<rebrand_spec>
**外部可见层全改 agent-builder：** UI 标题 / logo / favicon / page.json `name` / Docker image name / docker-compose service name / 环境变量前缀 `FLOCK_*` → `AGENT_BUILDER_*` / README.md / docs/
**内部代码层保留 flock：** Python import path（`from flock.xxx`）、内部类名/变量名/函数名含 flock 字样的**不动**
**目录树保持 flock 原结构不变**：所有新增模块作为新增 sub-package（不动 flock 原文件以避免 CLAUDE.md 2.3 fork discipline 检查）
**上游同步：** Never merge — fork = 快照，自此独立演进
</rebrand_spec>

<env_required>
Phase 1 必须在 .env.example 中预留：
- POSTGRES_DSN（asyncpg）
- POSTGRES_CHECKPOINT_DSN（psycopg3，给 LangGraph 用，Phase 2 用，先占位）
- REDIS_URL
- HMAC_SECRET（≥32 字符占位 `CHANGE_ME_AT_LEAST_32_CHARS_PLACEHOLDER`）
- JWT_SECRET（≥32 字符占位）
- SMTP_HOST / SMTP_PORT / SMTP_USERNAME / SMTP_PASSWORD / SMTP_FROM
- PUBLIC_BASE_URL（默认 http://localhost:8080）
- INTERNAL_BASE_URL（默认 http://localhost:8000）
- DEFAULT_FROM_EMAIL
- WEB_ORIGIN（前端 origin，CORS 用）
- LOG_LEVEL
- ENVIRONMENT（dev/staging/prod）
</env_required>

<docker_compose_spec>
6 个 service：
| Service | Image / Build | Port (host:container) | Healthcheck |
|---------|---------------|------------------------|-------------|
| postgres | postgres:16-alpine | 5432:5432 | pg_isready |
| redis | redis:7-alpine | 6379:6379 | redis-cli ping |
| api | build ./backend | (内部 8000，不直接暴露) | curl /health |
| worker | build ./backend (entrypoint=arq) | — | python -c "import arq" |
| web | build ./web | (内部 3000，不直接暴露) | curl / |
| nginx | nginx:alpine | 8080:80（内网管理端）、80:80（公网入口预留，Phase 1 实际只接 8080） | nginx -t |

依赖：api/worker → postgres + redis；web → api；nginx → api + web
</docker_compose_spec>

<test_baseline>
**pytest** (`backend/tests/`)：testcontainers-postgres + httpx.AsyncClient（不 mock DB）；至少一个 `test_smoke.py::test_health_endpoint` 跑通
**vitest** (`web/tests/`)：@testing-library/react；至少一个 `smoke.spec.ts` 跑通
**playwright** (`e2e/`)：base URL 指向 `${PUBLIC_BASE_URL:-http://localhost:8080}`；至少一个 `smoke.spec.ts::"打开首页看到 agent-builder 字样"` 跑通
</test_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork + Audit + Rebrand</name>
  <files>
    - backend/ (整个目录树，从 Onelevenvy/flock fork 而来，保留原结构)
    - web/ (整个目录树，rebrand 为 agent-builder-web)
    - .planning/phases/01-skeleton/FORK_AUDIT.md (新建)
    - scripts/bootstrap_fork.sh (新建，幂等 fork+rebrand 脚本)
    - .gitignore (追加 .env / *.key / *.pem / node_modules / __pycache__ / .pytest_cache / playwright-report / test-results)
  </files>
  <action>
    1. `git clone https://github.com/Onelevenvy/flock.git temp_flock`（如果还没 fork；用 https 公开 clone 即可，不需要 push 到我们的 repo）。
    2. 把 `temp_flock/backend/` 拷到本仓库 `backend/`；把 `temp_flock/web/`（或对应前端目录名）拷到本仓库 `web/`。**保留 flock 原目录结构不变**，所有内部 import path 保持 `from flock.xxx`。
    3. 创建 `.planning/phases/01-skeleton/FORK_AUDIT.md`，写入以下盘点（每条都执行命令验证）：
       - flock commit hash（git -C temp_flock rev-parse HEAD）
       - Python 版本要求（cat backend/pyproject.toml | grep python）
       - FastAPI 版本（cat backend/pyproject.toml | grep fastapi）
       - Pydantic v1/v2 状态（grep -r "pydantic.BaseModel" backend/ | head）
       - Next.js 版本（cat web/package.json | grep next）
       - 现有 NodeRegistry 节点类型列表（grep -r "register_node\|@register" backend/ | sort -u）
       - flock 已有的 docker-compose.yml / Dockerfile 内容摘要
       - **改造点清单（每项注明 keep / replace / hide）**：
         - flock 节点（RAG / Subgraph / MCP）：keep（保留代码 + 运行时隐藏，新 NodeRegistry 不注册）
         - flock 现有 auth：replace（v1 用我们自己的 JWT + bcrypt）
         - flock 现有 docker-compose：replace（我们重写适配 6-service）
         - flock 现有 UI 品牌：replace（agent-builder 字样替换）
    4. **执行外部可见层 rebrand**（用 ripgrep 找替换，不动 import path / 类名）：
       - web 端：`package.json` 的 `name` 字段、`<title>`、logo SVG（先放占位）、favicon、所有 hardcoded "Flock" 字符串替换为 "agent-builder"
       - 后端：`pyproject.toml` 的 `name` 字段、Docker image label、`FLOCK_*` env 前缀替换为 `AGENT_BUILDER_*`（grep -l "FLOCK_" backend/ 后 sed）
       - README.md / docs/ 全部重写为 agent-builder 文档（v1 写最简 README，docs/ 留空 placeholder）
       - **明确不动**：Python 模块名 `flock`、`from flock.xxx` import、类名含 flock 字样的、目录路径
    5. `scripts/bootstrap_fork.sh` 写成幂等脚本：detect → fork → rebrand → 输出 audit。CI 可重复运行不破坏现有状态。
    6. `.gitignore` 追加：`.env`、`*.key`、`*.pem`、`node_modules/`、`__pycache__/`、`.pytest_cache/`、`playwright-report/`、`test-results/`、`*.pyc`、`temp_flock/`、`.next/`。
    7. **fork discipline 自查**：用 `git diff --stat` 看本步骤产生的 diff，确保对 flock 原文件的修改仅限于品牌字符串（不动逻辑代码）。把 diff 摘要也写入 FORK_AUDIT.md。
  </action>
  <verify>
    <automated>
    test -d backend/ && test -d web/ &&
    test -f .planning/phases/01-skeleton/FORK_AUDIT.md &&
    test -f scripts/bootstrap_fork.sh &&
    grep -q "^\.env$" .gitignore &&
    grep -q "FLOCK_" backend/ 2>/dev/null; test $? -ne 0 || echo "warn: FLOCK_ residue in backend"
    </automated>
  </verify>
  <done>
    backend/ + web/ 目录存在；FORK_AUDIT.md 记录 flock commit/版本/改造点；rebrand 字符串替换完成；.gitignore 包含 .env 与 *.key；fork discipline 自查通过
  </done>
</task>

<task type="auto">
  <name>Task 2: docker-compose 6-service + Dockerfile + .env.example</name>
  <files>
    - docker-compose.yml (新建)
    - backend/Dockerfile (新建或覆写 flock 原版)
    - web/Dockerfile (新建或覆写 flock 原版)
    - nginx/Dockerfile (新建)
    - nginx/nginx.conf (新建占位，详细配置由 Plan 03 写)
    - .env.example (新建)
  </files>
  <action>
    1. `docker-compose.yml` 写 6 service：postgres:16-alpine / redis:7-alpine / api / worker / web / nginx；每个加 healthcheck（pg_isready / redis-cli ping / curl /health / nginx -t）；网络用单一 bridge `agent-builder-net`；volume 命名 `agent-builder-postgres-data` / `agent-builder-redis-data`。
    2. `backend/Dockerfile` 多阶段构建：base = python:3.11-slim，pip install 来自 backend/pyproject.toml（用 uv 或 pip，沿用 flock 已有选择）。安装 STACK.md 锁定的核心依赖（FastAPI 0.136.1 / SQLAlchemy 2.0.49 / asyncpg / PyJWT 2.12.1 / pwdlib[argon2]）。Entrypoint：`uvicorn flock.main:app --host 0.0.0.0 --port 8000 --workers 2`（保留 flock import path）。
    3. `web/Dockerfile`：node:22-alpine（Next.js 16 要求）；安装 pnpm 或 npm（用 flock 已选）；build + start。Dev override 用 `command: pnpm dev` 起 hot reload。
    4. `nginx/Dockerfile`：从 nginx:alpine 起，COPY nginx.conf 进去（详细配置 Plan 03 来写，本 plan 只写占位 `server { listen 80; location / { proxy_pass http://web:3000; } location /api/ { proxy_pass http://api:8000; } }`）。
    5. `.env.example`：按 <env_required> 全部列出，每条配 1 行中文注释。HMAC_SECRET / JWT_SECRET 占位写明 `CHANGE_ME_AT_LEAST_32_CHARS_PLACEHOLDER_xxxxxx`（恰好 32 字符）；POSTGRES_DSN 默认 `postgresql+asyncpg://postgres:postgres@postgres:5432/agent_builder`；PUBLIC_BASE_URL 默认 `http://localhost:8080`。
    6. 验证：`docker-compose config -q` 不报错；`docker-compose build --dry-run`（如不支持就 `docker-compose build api`）成功。
  </action>
  <verify>
    <automated>
    docker compose config -q &&
    test -f .env.example &&
    grep -q "HMAC_SECRET" .env.example &&
    grep -q "JWT_SECRET" .env.example &&
    grep -q "POSTGRES_DSN" .env.example &&
    grep -q "PUBLIC_BASE_URL" .env.example &&
    test -f backend/Dockerfile &&
    test -f web/Dockerfile &&
    test -f nginx/Dockerfile
    </automated>
  </verify>
  <done>
    `docker compose config` 通过；6 个 service 都有定义 + healthcheck；.env.example 含全部 Phase 1 必需变量
  </done>
</task>

<task type="auto">
  <name>Task 3: 三层测试 runner 基线 + smoke 用例</name>
  <files>
    - backend/tests/conftest.py (新建)
    - backend/tests/test_smoke.py (新建)
    - backend/pyproject.toml (加 dev deps: pytest / pytest-asyncio / testcontainers-postgres / httpx)
    - web/tests/smoke.spec.ts (新建)
    - web/vitest.config.ts (新建或更新)
    - e2e/playwright.config.ts (新建)
    - e2e/smoke.spec.ts (新建)
    - e2e/package.json (新建，独立项目装 @playwright/test)
    - scripts/run_all_tests.sh (新建，一条命令跑三层 smoke)
  </files>
  <action>
    1. **pytest 基线**：
       - `backend/tests/conftest.py`：定义 fixtures
         - `postgres_container`（session scope，testcontainers-postgres 起 postgres:16，导出 DSN）
         - `redis_container`（session scope，testcontainers-redis）
         - `async_client`（function scope，AsyncClient 包 ASGITransport(app)）
         - `db_session`（function scope，AsyncSession，事务回滚保证隔离）
       - `backend/tests/test_smoke.py`：`async def test_health_endpoint(async_client): r = await async_client.get("/health"); assert r.status_code == 200; assert r.json()["status"] == "ok"`
       - `backend/pyproject.toml` dev deps 加：pytest>=8.0、pytest-asyncio>=0.24、testcontainers>=4.0、httpx>=0.28、pytest-cov>=5.0
       - 配置 pytest-asyncio mode = auto
    2. **vitest 基线**：
       - `web/vitest.config.ts`：jsdom env、@testing-library/react 集成
       - `web/tests/smoke.spec.ts`：`it("renders agent-builder 字样", () => { render(<Page />); expect(screen.getByText(/agent-builder/i)).toBeInTheDocument(); })`
       - `web/package.json` 加 test 脚本：`"test": "vitest run"`
    3. **playwright 基线**：
       - `e2e/package.json`：独立项目，装 `@playwright/test@latest`
       - `e2e/playwright.config.ts`：projects = chromium / webkit；baseURL 从 `process.env.PUBLIC_BASE_URL ?? 'http://localhost:8080'`；reporter = html + list；webServer 配置（dev 模式自动起 docker-compose）
       - `e2e/smoke.spec.ts`：`test("打开首页看到 agent-builder 字样", async ({ page }) => { await page.goto('/'); await expect(page).toHaveTitle(/agent-builder/i); })`
    4. **一条命令跑全部 smoke**：
       - `scripts/run_all_tests.sh`：sequentially 跑 `pytest backend/tests/test_smoke.py -x`, `cd web && npm run test`, `cd e2e && npx playwright test smoke.spec.ts`
       - 输出汇总：哪一层通过 / 失败
    5. **测试 isolation**：保证三层都不依赖 docker-compose 实际启动（用 ASGITransport 直连 FastAPI app；vitest 是纯前端；playwright 需要 docker-compose up，所以 smoke 在本 plan 暂时 mark `test.skip` 或在 README 标注"需要先 docker-compose up"，避免 CI 误判）。
    6. **明确写入 web/tests/smoke.spec.ts 与 e2e/smoke.spec.ts 顶部注释**：本 plan 只验证 testing infra 起来，业务 E2E 由 Plan 06 编写。
  </action>
  <verify>
    <automated>
    cd backend && python -m pytest tests/test_smoke.py -x --co 2>&1 | grep -q "test_health_endpoint" &&
    cd ../web && npx vitest run tests/smoke.spec.ts --reporter=verbose 2>&1 | grep -q "agent-builder" &&
    test -f ../e2e/playwright.config.ts &&
    test -f ../e2e/smoke.spec.ts &&
    test -f ../scripts/run_all_tests.sh && test -x ../scripts/run_all_tests.sh
    </automated>
  </verify>
  <done>
    pytest 收集到 smoke 测试函数；vitest 跑通 smoke；playwright config 与 smoke spec 已写就绪；scripts/run_all_tests.sh 可执行
  </done>
</task>

</tasks>

<verification>
**Plan-level checks (在所有 task 完成后跑)：**
1. `docker compose config -q` 通过
2. `docker compose up -d postgres redis` 起来，pg_isready 通过
3. `bash scripts/run_all_tests.sh`：三层 smoke 全过（playwright 标 skip 也算）
4. `git diff --stat HEAD~1`：对 flock 原文件的修改 < 10%（CLAUDE.md fork discipline）
5. `grep -r "Flock\|flock " web/public web/app 2>/dev/null`：外部可见层无 "Flock" 残留
</verification>

<success_criteria>
1. docker-compose 文件 + 各 service Dockerfile + .env.example 全部就位
2. FORK_AUDIT.md 完整记录 flock 当前状态与改造点
3. pytest + vitest + playwright 三个 runner 都能跑通 smoke
4. fork discipline 自查通过：未修改 flock 原文件逻辑代码
5. Plan 02/03/04/05 可以并行启动开发（依赖本 plan 的 docker-compose 与测试基线）
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton/01-01-SUMMARY.md` documenting:
- flock commit hash forked from
- rebrand 替换清单
- docker-compose 各 service 端口与依赖
- 三层测试 runner 配置位置
- FORK_AUDIT.md 关键发现（Pydantic 版本、Next.js 版本等）
</output>
